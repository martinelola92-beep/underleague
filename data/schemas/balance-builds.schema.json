{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://underleague.dev/schemas/balance-builds.schema.json",
  "title": "Underleague balance build",
  "description": "data/balance/builds/<id>.json: una build de prueba para /Balance (docs/fase1-diseno.md §8). 'slot' es el índice de titular que produce TeamGenerator: 0 portero, 1-2 defensas, 3-5 centrocampistas, 6 delantero. La validación de build (§5) la hace Simulator.Run al construir el partido, no este esquema.",
  "type": "object",
  "properties": {
    "_doc": {
      "type": "string"
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*$"
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "race": {
      "enum": [
        "Human",
        "Orc",
        "Elf",
        "Dwarf",
        "Undead",
        "DarkElf",
        "Demon",
        "Vampire",
        "Lizard"
      ]
    },
    "quality": {
      "type": "integer",
      "minimum": 1,
      "maximum": 99,
      "description": "Media objetivo de atributos del equipo (dial de fuerza, paquete U)."
    },
    "perks": {
      "type": "array",
      "description": "Asignaciones de perk a titular. Un mismo slot puede aparecer varias veces (varios perks).",
      "items": {
        "type": "object",
        "properties": {
          "slot": {
            "type": "integer",
            "minimum": 0,
            "maximum": 6
          },
          "perk": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*$"
          }
        },
        "required": [
          "slot",
          "perk"
        ],
        "additionalProperties": false
      }
    },
    "rarities": {
      "type": "object",
      "description": "Sube la rareza de un titular (y con ella sus slots de perk, RF-023). Clave: índice de slot como texto.",
      "propertyNames": {
        "pattern": "^[0-6]$"
      },
      "additionalProperties": {
        "enum": [
          "common",
          "rare",
          "legendary"
        ]
      }
    },
    "lineup": {
      "description": "Casillas-hogar relativas por slot ([columna, fila]); null usa Lineup.Default.",
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "array",
          "items": {
            "type": "array",
            "prefixItems": [
              {
                "type": "integer",
                "minimum": 0,
                "maximum": 7
              },
              {
                "type": "integer",
                "minimum": 0,
                "maximum": 4
              }
            ],
            "items": false,
            "minItems": 2
          },
          "minItems": 5,
          "maxItems": 7
        }
      ]
    },
    "level": {
      "type": "integer",
      "minimum": 1,
      "maximum": 8,
      "description": "Nivel de todos los jugadores de la plantilla (RF-023). Por defecto 1."
    },
    "rarity": {
      "enum": [
        "common",
        "rare",
        "legendary"
      ],
      "description": "Rareza uniforme de los diez jugadores; 'rarities' sigue pudiendo subir slots concretos. Ausente = RF-005 (un raro entre diez)."
    },
    "styles": {
      "type": "object",
      "description": "Instrumento de medida: impone la etiqueta de estilo de un titular (ADR 0024) en vez de sortearla, para las builds que prueban un perk con tagsRequired sobre una etiqueta de estilo. Clave: índice de slot como texto.",
      "propertyNames": {
        "pattern": "^[0-6]$"
      },
      "additionalProperties": {
        "enum": [
          "Brute",
          "Fine",
          "Bulwark",
          "Cold",
          "Neutral"
        ]
      }
    },
    "traits": {
      "type": "object",
      "description": "Instrumento de medida: rasgos que se añaden a un titular, para las builds que prueban un perk con tagsRequired sobre un rasgo. Clave: índice de slot como texto.",
      "propertyNames": {
        "pattern": "^[0-6]$"
      },
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "string",
          "pattern": "^[A-Z][A-Za-z]*$"
        },
        "uniqueItems": true
      }
    },
    "counters": {
      "type": "object",
      "description": "Instrumento de medida (paquete Y): contadores de carrera con los que entra un titular, para los perks accumulatesAcrossMatches (RF-070). Es lo que separa 'buena' de 'muy buena' en la escala de la ADR 0033: la misma build con su escalado alimentado o con el escalado acumulado durante toda la run. Clave: índice de slot como texto.",
      "propertyNames": {
        "pattern": "^[0-6]$"
      },
      "additionalProperties": {
        "type": "object",
        "propertyNames": {
          "pattern": "^[a-zA-Z][a-zA-Z0-9]*$"
        },
        "additionalProperties": {
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "items": {
      "type": "object",
      "description": "Objeto de equipamiento de un titular (RF-075..078): clave = índice de slot como texto, valor = id de data/items/. Es la otra mitad de \"muy buena\" en la ADR 0033 (\"buena, ADEMÁS equipada\"): sin este campo la fila superior de la curva se medía sin equipamiento. Un jugador lleva un objeto como máximo (RF-076).",
      "propertyNames": {
        "pattern": "^[0-6]$"
      },
      "additionalProperties": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9_]*$"
      }
    }
  },
  "required": [
    "id",
    "name",
    "race",
    "quality",
    "perks"
  ],
  "additionalProperties": false
}
