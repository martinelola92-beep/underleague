# Decisiones pendientes e inconsistencias

Registro vivo. Cuando una decisión se toma, se mueve a un ADR en `decisiones/` y aquí queda una línea con el enlace. Las inconsistencias se resuelven actualizando `requisitos.md` a una versión nueva.

## Decisiones abiertas (§10 de requisitos)

| Id | Decisión | Bloquea | Lectura provisional |
|---|---|---|---|
| D-1 | Nombre del proyecto y del universo | Fase 4 | Repositorio: **Underleague** |
| D-2 | Número exacto de nodos por acto y distribución por tipo | Fase 2 | **Resuelta 2026-09-05 (paquete Z, Z-20)**: `data/map/map.json` -> `nodesPerAct: [11, 12, 12]`. Son 35 nodos por run (RF-003b pide 30-36) y **20 partidos** en el peor camino —6 en el acto 1, 7 en el 2 y en el 3, jefe incluido—, el 57,1% de los nodos, por debajo del tope del 60%. Con 11 en los tres actos salían 18 partidos, justo en el borde de la banda "18-22" de §10 |
| D-3 | Modelo económico del salario de mercenarios frente al coste de tienda | Fase 2 | **Resuelta 2026-09-05 (paquete Z, Z-21)**: el mercenario no cuesta fichaje, cuesta salario. `mercenaryBaseWage` 16 + 12 por escalón de rareza: uno raro cuesta 28 por partido y 196 en un acto de siete, algo más que un objeto común y comparable a una clínica (180). Es la relación que RF-114k necesita para que los cuatro sumideros sean del mismo orden. **Anotado**: con trece jugadores en plantilla la política automática nunca ficha uno, así que el sumidero está calibrado pero no se ejercita (`docs/balance/fase2-resultados.md` §5). **Reescalado por la ADR 0044** (paquete AB): base 1 + 1 por escalón de rareza, es decir 2 por partido un mercenario poco común y 14 en un acto de siete, frente a una clínica de 10 y un acto que gana 23-47. La relación entre los cuatro sumideros se conserva; lo que cambia es la escala |
| D-4 | Si la turba introduce peligros de público como entidades o solo estrechamiento | Fase 3 | Solo estrechamiento + casillas invadidas fijas (RF-055b). Nota: el enunciado original habla de "fase 3 del partido", concepto eliminado por RF-055 |
| D-5 | Qué 3 razas entran en el prototipo | Fase 0 | **Resuelta 2026-09-03**: humanos, orcos, elfos |
| D-6 | Si el jefe final admite reintento inmediato o exige nueva run | Fase 2 | **Resuelta 2026-09-05 (paquete Z, Z-22)**: nueva run. Se confirma la lectura provisional: el guardado ironman se borra al cargar (RT-061) y `RunEngine` graba el desenlace en el estado, así que un reintento exigiría conservar un estado que la propia regla de guardado destruye. No hay valor en datos: la decisión es que **no existe** ninguna clave de reintento |
| D-7 | Detalle de la tienda de Rune Dice a replicar | Fase 2 | **Resuelta 2026-09-05 (paquete Z, Z-23)**: solo la estructura por categorías. Cuatro categorías simultáneas (RF-114) con 3-4 artículos, surtido derivado de la semilla del nodo (W-12) y sin renovación. No se replican ni reroll del surtido, ni cupones, ni descuentos, ni artículos que modifiquen la tienda. Valor en datos: bloque `market` de `data/economy/economy.json` con `playerOffers` 3, `perkOffers` 4, `itemOffers` 4 y `consumableOffers` 3 (15-16 artículos, el tamaño que fija la ADR 0037) |
| D-8 | Qué logro desbloquea cada raza y en qué orden | Fase 4 | Ejemplos de RF-125 |
| D-9 | Si el jefe final introduce condición de derrota propia y cuál | Fase 2 | **Resuelta (paquete Y).** `BossDefeatConditionKind.DrawIsDefeat` (`Sim/Run/Boss/BossDefinition.cs`/`BossRules.cs`): si el partido llega empatado al final del reglamentario, cuenta como derrota aunque se gane el gol de oro. Solo el jefe del acto 3 la lleva (`data/bosses/eternal_crown.json`, "El campeón conserva el título"); `grimhold_guns` y `the_hunt` no. Medido: anula entre el 13% y el 20% de las victorias en el campo (`bossGateDefeatCondition_eternal_crown_*`). Esta fila no se había actualizado tras implementarla |
| D-10 | Distribución exacta de tipos de nodo dentro de cada acto | Fase 2 | **Resuelta 2026-09-05 (paquete Z, Z-20)**: se deriva de D-2 por construcción en `MapGenerator`, no se declara aparte. Con 11 nodos: capas de mercado en la 2, la 5 y la 8 (un solo nodo cada una, RF-011b por construcción, W-2), jefe en la última, 5 capas de partido y 2 de servicio; con 12, 6 de partido y 2 de servicio. Cada capa libre es entera de partidos o entera de servicios, de modo que el tope del 60% se cumple sobre el peor camino y no en promedio (W-3) |
| D-11 | Precio de lanzamiento y alcance de la demo | Fase 4 | — |

## Decisiones añadidas durante la preparación del repositorio y la fase 0

| Id | Decisión | Bloquea | Lectura provisional |
|---|---|---|---|
| D-12 | Idioma de los identificadores de C# | Fase 0 | **Resuelta 2026-09-03**: inglés (ADR 0009, `glosario-identificadores.md`) |
| D-17 | `/Game` con `net10.0` en Godot 4.6 (ADR 0008) | Fase 1 | **Resuelta 2026-09-05**: verificado con Godot 4.6.3 mono, compila y ejecuta; no hace falta bajar a `net8.0` |
| D-13 | `/Run` como librería separada de `/Sim` o namespace dentro | Fase 2 | Namespace `Underleague.Sim.Run` hasta la fase 2 (ver `arquitectura.md`) |
| D-14 | Organización de `/data`: un fichero por entidad o uno por tipo | Fase 1 | Un fichero por entidad; facilita revisión y diffs |
| D-15 | Cómo se resuelve el consumible manual sin romper la pureza de `Simulador.Ejecutar` | Fase 1 | Re-ejecución con activación en el estado (ver `arquitectura.md`) |
| D-16 | Analizador para prohibir APIs no deterministas en `/Sim` | Fase 0 | **Resuelta 2026-09-03**: implementado — `Microsoft.CodeAnalysis.BannedApiAnalyzers` en `Sim.csproj` con `Sim/BannedSymbols.txt` (System.Random, DateTime, DateTimeOffset, Guid, Stopwatch, Environment.TickCount(64), IO.File, IO.Directory, Parallel, HashCode), más el test de ensamblado ya existente (`ArchitectureTests`) |
| D-18 | `Trait.Leader` está implementado (§3.5) pero **inerte con `Lineup.Default`**: las casillas-hogar por defecto (GK (0,2); DEF (2,1),(2,3); MID (4,0),(4,2),(4,4); FWD (6,2)) no son contiguas entre sí, así que ningún jugador tiene un Leader adyacente y el bono nunca se aplica en fase 0 | Fase 1 | Se deja implementado y probado con una alineación a mano (`UtilityTests.LeaderBonusMultipliesTheBaseScore`). Cuando la fase 1 permita colocar a los titulares, hay que volver a medir su efecto en `/Balance` antes de darlo por equilibrado; si entonces sigue sin usarse, se retira de `traits.json` y de `TraitDefinition` |
| D-19 | `individualDeviation` de las razas bajado de 12/14 a 8/9 en el paquete E | Fase 1 | La dispersión anterior hacía que la plantilla concreta pesara más que la diferencia de calidad en `betterTeamWinRate` (desviación de 11 puntos entre semillas). Con 8/9 baja a ~6 puntos y el rango 65-80 es alcanzable de forma estable. Revisar cuando existan niveles y perks, que añaden su propia dispersión |
| D-20 | `generation.positionBias.leash` sigue en `tuning.json` y sí se usa, pero el atributo `leash` ya no depende de `quality` ni del dado (§2.6) | Fase 1 | La correa es disciplina posicional. Si en fase 1 se quiere que un jugador mejor cubra más campo, hazlo con una palanca continua (por ejemplo, casillas en centésimas) y no con la conversión entera actual, que da saltos de una casilla completa |
| D-21 | Penaltis casi inalcanzables en fase 0 | Fase 1 | La falta debe ocurrir dentro del área del que entra y los defensas viven en la columna 2; `referee.penaltyOnFoulInArea` no tiene efecto medible (4 penaltis en 500 partidos). Revisar en fase 1 con la colocación libre |
| D-22 | `RefereeSetup.Trait` se valida pero el motor lo ignora en fase 0 | Fase 3 | Árbitro neutro por diseño en fase 0 (ningún `RefereeTrait` distinto de `Neutral` cambia una probabilidad); implementar en fase 3 |
| D-23 | `bloodlust` es un **perk muerto por construcción**: su condición `hasTag(actor,'Brute') && bias() < 0` no puede ser cierta mientras el árbitro sea fijo y neutro y ningún perk use `modifyBias` (`bias()` vale 0 siempre). Medido: 0 activaciones en 2.666 partidos | Fase 3 | Se deja como está (es una condición falsa, no un fallo del motor). Al implementar el criterio del árbitro (RF-060, fase 3) volverá a tener sentido; si entonces sigue sin dispararse, cambiar la condición a `scoreDiff() < 0` o retirarlo |
| D-24 | `severeInjury` **no tiene ninguna consecuencia en fase 1**: no hay muertes (RF-093) y "grave" solo cambia el `Detail` del evento. `guardian_angel` se activa en el 100% de los partidos y no cambia ni un resultado (medido: 0 puntos de tasa de victoria con `severeInjury +1500` en toda la plantilla) | Fase 3 | Todo perk sobre `severeInjury` es letra muerta hasta que la lesión grave tenga efecto (baja de N partidos, prótesis, muerte). Revisar el catálogo entonces |
| D-25 | El **atributo `speed` casi no valía nada**: +10 de velocidad a toda la plantilla movía la tasa de victoria 0,4 puntos | — | **Resuelta 2026-09-04 (paquete U)**: con los cuerpos con volumen y `FindSpace`, +20 de velocidad valen **+6,6 puntos**, el tercero de los cinco atributos. No hizo falta tocar ninguna fórmula: llegar antes a un sitio ahora significa ocuparlo. Curvas en `docs/balance/fase1b-resultados.md` §3 |
| D-26 | Los efectos de perk sobre **probabilidades saturadas no valen nada**: el pase base estaba en 9.200/10.000 y el regate en 8.400, así que un `+800` de pase o un `+1200` de regate se perdía casi entero, mientras que el mismo valor en negativo sí mordía | — | **Resuelta 2026-09-04 (paquete U)**: `pass.baseSuccess` baja a 7.700 y `dribble.baseWin` a 7.200, así que los dos canales tienen recorrido en los dos sentidos, y el catálogo se reescribe eligiendo el canal por su recorrido. Queda abierta la parte contraria, D-30: la escala única de 5/10/15/20/25 puntos porcentuales es **demasiado gruesa** para `intercept` y `injure` |
| D-27 | **La medida de builds de fase 1 era ruido de generación.** Con una plantilla por build, la misma build contra su referencia daba entre el 16,5% y el 59,5% (sd 14,9 puntos). Corregido en `/Balance` (plantillas emparejadas, `--rosters`, reparto de ids alternado) | — | **Resuelto 2026-09-04**. Cualquier número de balance de builds anterior a esta corrección hay que descartarlo |
| D-28 | La segunda mitad de `scalingRewardsGoodBuilds` ("las builds malas caen ≥ 15 puntos entre la primera y la segunda mitad de la campaña") **no es alcanzable** | Fase 2 | **Recomprobada 2026-09-04 (paquete U)** con el motor nuevo y con el dial de calidad arreglado, que ahora sube al rival 14 puntos **por atributo** en ocho partidos. Con un control sin ningún perk en la medición, el resultado es el contrario del que pide la métrica: **quien más cae es `human_none` (−13,75)**, y las builds malas caen entre 2,5 y 0,0. Una build mala cae **menos** que la referencia porque hasta ella lleva algún perk que funciona, y porque un perk malo es un **malus estático** que cuesta lo mismo en el partido 1 que en el 8. Tres salidas en `docs/balance/fase1b-resultados.md` §8.3: reformular la métrica contra la caída de la propia referencia (ADR, sin código), el desgaste persistente de la fase 2 (RF-090..094), o `elseEffects` con contador que hagan el malus creciente. La primera mitad **sí se cumple**: ninguna coherente cae más de 5,8 puntos |

| D-29 | **El equilibrio entre razas no lo mide ninguna puerta.** Sin perks, calidad 50, contra `human_none`: elfos 68,5%, orcos 58,3%, enanos 50,8%, humanos 50%, no-muertos 48,5%. Veinte puntos de diferencia; caben en la banda de RT-055 pero los elfos están claramente por delante. La puerta de fase 1 es ciega a esto porque compara cada build contra la referencia **de su propia raza** | Fase 1 | **Corregida 2026-09-04, pendiente de D-35 para cerrarse.** Puerta: `Sim.Tests/Analysis/RaceBalanceTests.cs` (categoría `Gate`), todas-contra-todas entre las cinco referencias `*_none`, 250 plantillas × 4 partidos × 10 parejas, banda 40%-60% de tasa agrupada. **Ni los atributos ni el cuerpo eran la causa**: `attributeBias` se aplica antes de renormalizar al presupuesto de generación (`PlayerGenerator.GenerateAttributes`, pasos 3-5), así que reparte puntos en vez de añadirlos y como palanca de balance casi no mueve nada; medido con las habilidades raciales apagadas, los sesgos actuales dejan a las cinco razas en 3,5 puntos (enanos 49,5 · elfos 51,0 · humanos 49,3 · orcos 51,8 · no-muertos 48,3). `discipline` es inerte a efectos de tasa de victoria y `bodyRadius` no se puede mover sin romper `BodiesTests`. La causa era **el presupuesto de las habilidades raciales**, que la ADR 0026 nunca fijó: repartía un canal distinto a cada raza pero no cuánto podía valer cada una. Medido apagando cada habilidad (1.000 plantillas por pareja, 40.000 partidos): Toque **+10,4** puntos, Sangre caliente +0,9, Raíces +0,6, Adaptables y No sienten nada 0 por construcción (actúan fuera del partido). Y la mitad de intercepción de Toque valía ella sola **+6,6** porque no era un número mal calibrado sino un **canal ilegal**: `intercept` tiene base 250 y el escalón mínimo de la escala de puntos porcentuales son 500, así que el valor más pequeño que se puede escribir no es "esquivar mejor" sino "ser inmune" (es D-30 en su forma más pura). **Criterio de presupuesto fijado en la ADR 0026, §"Presupuesto de impacto"**: (1) canal legal antes que valor —una habilidad racial no puede vivir en un canal cuya base sea menor que el escalón mínimo—; (2) techo de **+2,5 puntos** de tasa agrupada, que es lo que vale un escalón de la escala sobre un canal con recorrido (+1,7 medido) más el ruido; (3) las habilidades que actúan fuera del partido valen 0 dentro y su presupuesto es de campaña, lo mide la fase 2. **Cambios**: `elf_touch` pierde `interceptEvasion` y baja `tackleEvasion` de 10 a 5 (+10,4 -> +1,7); `hot_blooded` sube de 5 a 15 ticks de derribo para llegar al mismo techo (+0,9 -> +1,4); `roots`, `numb` y `quick_learner` sin cambios, ya estaban dentro; y `data/races/elf.json` **recupera su sesgo de atributos** (`{ strength -12, speed 6, technique 14, stamina -6, leash 1 }`) y su `discipline` 35, que un ajuste anterior había borrado para compensar Toque —el sesgo describe a la raza (tabla §3.4: "Técnica, precisión"), no es una palanca de balance—. **Resultado** (250 plantillas, semilla 1): enanos 47,55 · elfos 54,12 · humanos 48,95 · orcos 51,80 · no-muertos 47,58; margen más ajustado **5,9 puntos** contra el techo, frente a menos de 1 antes. Con 1.000 plantillas: 49,07 · 52,89 · 48,23 · 52,48 · 47,34. La descripción a mano de `elf.json` vuelve a ser verdad al restaurarse el sesgo y no se toca. **Queda para el revisor** (fila R-9): RF-031b y la tabla §3.4 siguen diciendo que los elfos esquivan también las intercepciones; esa mitad vuelve cuando D-30 ponga el canal `intercept` en escala. **CERRADA.** El bloqueo con D-35 ya no existe: `elf_out_of_zone` perdió sus tres `center_conductor` en el commit `3ee9bd9` (opción (a) de D-35), antes incluso del reequilibrio racial de más arriba, y esta fila nunca se había actualizado para reflejarlo. `BuildGateTests.BadBuildsLoseToTheirBaseline` verificado en verde el 2026-09-09 (15 m, 1/1) sobre el árbol actual |
| D-34 | **RT-055 no se cumple y el problema es el punto de comparación.** Las nueve builds coherentes ganan entre el 68,3% y el 86,3% a `human_none`, por encima del techo del 70%. No es que estén rotas: `human_none` es una plantilla con **cero** perks y una build con los siete titulares equipados lleva **catorce** (RF-023, dos slots por común). Comparar un equipo completo contra uno sin equipar no mide equilibrio, mide cuánto vale el catálogo entero | Fase 2 | Cambiar el criterio de RT-055 **exige un ADR** (RT-057): o la referencia pasa a ser una build de perks neutros en vez de una plantilla desnuda, o se ensancha la banda. Mientras tanto, la puerta que sí mide el diseño de una build es la de fase 1, que compara contra la referencia de su propia raza |
| D-30 | **La escala de valores de perk no cabe en una sola tabla.** `fase1b-diseno.md` §1.4 fija 5/10/15/20/25/50 puntos porcentuales para todo `modifyProbability`, pero los canales no viven en la misma escala: +5 pp **triplican** `intercept` (base 250) y multiplican por 13 `injure` (base 40 en entrada limpia), mientras que en `pass` (base 7.700) son un +6% relativo. El paso mínimo es a la vez demasiado grande y demasiado pequeño | Fase 2 | En el paquete U se ha resuelto a mano (los perks de intercepción y de lesión están todos en 5 o en 10). Para el catálogo de lanzamiento: o la escala se expresa en **múltiplos de la base del canal**, o se suben las bases de `intercept` e `injure`. Las dos exigen ADR |
| D-31 | **La correa sigue sin comprar nada.** Con la zona de acción de la ADR 0028, +20 de correa valen **−1,6 puntos** de tasa de victoria (eran −5,1 antes de estrechar `scaleFromLeashPercent` a 85/115): una zona grande deshace la estructura del bloque y no da nada a cambio. El presupuesto de generación le dedica entre el 10% y el 18% de sus puntos, así que cada punto de calidad que compra correa se tira | Fase 2 | Dos salidas, las dos de diseño: sacar la correa del presupuesto y volverla un descriptor posicional (modifica las ADR 0025 y 0028), o darle un canal positivo medible (llegar a balones sueltos fuera de la zona, cubrir carril de pase), que es mecánica nueva |
| D-32 | **Mover un atributo a la decisión puede volverlo negativo.** Con `longPassTechniqueSlope` en 16, un equipo con +20 de técnica ganaba el **40,9%**: prefería el pase largo, lo perdía y dejaba de rematar (1,01 goles a favor frente a 1,4). Con la pendiente en 10 y el pago movido a la resolución sube a +7,5 | — | **Anotado, no abierto.** Es la lección práctica de la ADR 0030 y hay que tenerla delante al añadir pendientes por atributo: una pendiente de decisión solo mejora si la acción que induce es mejor que la que sustituye, y eso hay que medirlo |
| D-33 | **`Trait.Leader` deja de ser inerte** (cierra la mitad de puerta de D-18): con la alineación por defecto 2-3-1 del paquete U los vínculos direccionales se resuelven y `natural_leader` se activa en el 100% de los partidos de `human_counter`. Falta medir el bono de adyacencia del rasgo por separado | Fase 2 | Medirlo con `--rosters` cuando la fase 2 permita colocar a los titulares desde la interfaz |
| D-35 | **CERRADA, y estaba resuelta desde antes del reequilibrio racial que la citaba como bloqueo.** `elf_out_of_zone` no era una build mala, era una build con `center_conductor` ×3: un perk que da `intercept` +5 a todo el equipo, sobre un canal de base 250, multiplicado por siete con tres copias. Medía ≤45% contra `elf_none` solo porque la inmunidad racial élfica a las intercepciones cancelaba esa arma, y D-29 iba a retirar esa inmunidad | Fase 1 (puerta en rojo) | **CERRADA.** La salida (a) —quitar los tres `center_conductor` de `data/balance/builds/elf_out_of_zone.json`— se aplicó en el commit `3ee9bd9 data: elf_out_of_zone deja de llevar tres copias de center_conductor (D-35)`, **antes** del commit `562d408` que trae el reequilibrio racial de D-29. El fichero actual no lleva ningún `center_conductor`; esta fila y la de D-29 seguían marcadas como bloqueo mutuo por wording no actualizado, el mismo patrón que D-9. `BuildGateTests.BadBuildsLoseToTheirBaseline` verificado en verde el 2026-09-09 |

## Inconsistencias detectadas en requisitos v0.9

Se aplica la lectura indicada hasta que el documento suba de versión. **I-1 a I-5 e I-9 quedan incorporadas al texto de `requisitos.md` en v0.9.1**; para el resto sigue aplicándose la lectura de esta tabla.

| Id | Dónde | Conflicto | Lectura aplicada | Estado |
|---|---|---|---|---|
| I-1 | RT-030 vs RF-022 | El esquema lista `precision` y `agresividad` como atributos; RF-022 integra precisión en técnica y expresa agresividad como rasgo | Cinco atributos: fuerza, velocidad, técnica, resistencia, correa | Corregida en v0.9.1 |
| I-2 | RT-030 vs RF-076 | `equipamiento[] (ids, por slot)` frente a "un único objeto equipado" | Un campo `equipo` con un id o null | Corregida en v0.9.1 |
| I-3 | RT-030 vs RF-103 | `vinculos[] (…, signo)` frente a "no existen vínculos negativos en el lanzamiento" | Sin campo `signo`; se añade si entran rivalidades | Corregida en v0.9.1 |
| I-4 | Glosario vs RF-055 | Glosario: "Fase: uno de los tres tramos en que se divide un partido"; RF-055: el reglamentario es una sola fase | El partido tiene reglamentario + turba opcional. "Fase" se reserva para los tramos de una **jugada** (RF-051) y para el plan de desarrollo | Corregida en v0.9.1 |
| I-5 | RT-033 vs RF-055 | Ejemplo de perk con `limite.por = "parte"`; no existen partes | Ámbitos válidos: `jugada`, `partido`, `turba`, `run` | Corregida en v0.9.1 |
| I-6 | RF-128 | "Una run recorre los tres actos de una división (C, B, A)" frente a la tabla con cinco divisiones Tercera..Mundial | Cinco divisiones con los nombres de la tabla | Abierta |
| I-7 | RT-056 vs RF-055c | "Menos del 15% de empates" frente a "los empates no existen como resultado final" | La métrica mide empates **al final del reglamentario**, es decir, frecuencia de turba | Abierta |
| I-8 | D-9 vs RF-001c | La decisión pendiente pregunta si existe la condición de derrota propia del jefe final; RF-001c ya la afirma | Existe; falta definirla | Abierta |
| I-9 | RF-066 vs RF-055b | El catálogo tiene `INICIO_TURBA` y `ARBITRO_SE_VA` como eventos distintos aunque ocurren a la vez | Se emiten ambos, en ese orden, en el mismo tick | Corregida en v0.9.1 |
| I-10 | Numeración | Secciones 3.6c antes de 3.6b, 3.12b/3.12c | Cosmético; se corrige al subir de versión | Abierta |
| I-11 | RT-056, fila de resultados | `< 15% de empates al final del reglamentario` es incompatible con `mayoría de resultados entre 1-0 y 3-2` y `< 5% con más de 5 goles`: con marcadores casi independientes, bajar del 15% de empates exige unos 8 goles por partido (`e^-2λ·I₀(2λ)`) | La métrica se emite como `INFO` y no bloquea la puerta; medida en 29-31% con 2,4 goles por partido. Resolver con un ADR: o se sube el rango a ~30%, o se introduce una mecánica que correlacione los marcadores (el equipo por detrás arriesga más) | — (no forma parte del paquete P) |
| I-12 | `balance.md` vs `fase0-diseno.md` §4 | `balance.md` pedía "equipo +10 en todos los atributos gana 65-80%"; `fase0-diseno.md` §4 y `Balance/Metrics.cs` aplican ese rango a una diferencia de **20** y dejan la de 10 como `INFO` | Se aplica la lectura de §4 (diferencia 20 obligatoria, diferencia 10 informativa en 55-70%) y se corrige la tabla de `balance.md` en el mismo commit. Con los datos del paquete E, +20 gana 73,0% y +10 gana 65,8% en 2.000 partidos con semilla 1 | — (no forma parte del paquete P) |

## Cambios de requisito pendientes de subir `requisitos.md` de versión

Todos los cambios R-1 a R-7 quedan **aplicados en `requisitos.md` v0.9.1** (2026-09-04, paquete P). La tabla se conserva como registro histórico.

| Id | Requisito | Cambio | Origen | Estado |
|---|---|---|---|---|
| R-1 | RF-024 | Se invierte: el legendario es netamente superior y un común de nivel máximo equivale a un legendario de nivel 2, en vez de superarlo | ADR 0027 | Aplicado en v0.9.1 |
| R-2 | RF-023b | Se matiza: el común sigue siendo competitivo **si el jugador lo cuida** (nivel, perks acumulativos, vínculos), no por defecto | ADR 0027 | Aplicado en v0.9.1 |
| R-3 | RF-022d | La etiqueta deja de ser única por raza: se separa en etiqueta de especie (fija) y etiqueta de estilo (individual, con sesgo racial) | ADR 0024 | Aplicado en v0.9.1 |
| R-4 | RF-044 | La adyacencia se resuelve antes del partido en vínculos direccionales acotados, no como condición evaluada en cada evento | ADR 0021 | Aplicado en v0.9.1 |
| R-5 | RF-031 | Cada raza tiene una habilidad concreta, implementada como perk de equipo | ADR 0026 | Aplicado en v0.9.1 (nuevo RF-031b, y columna de habilidad en la tabla de razas de §3.4) |
| R-8 | RF-057 | Se permite el contacto con un rival sin balón siempre que esté dentro de la jugada activa (radio alrededor del balón o corredor hacia la portería atacada); sigue prohibida la pelea paralela | ADR 0030 |
| R-7 | RF-045 | En la colocación se muestra la zona del jugador manipulado con sus dos capas, no todas las correas a la vez, que con zonas asimétricas serían ilegibles | ADR 0029 | Aplicado en v0.9.1 |
| R-6 | RF-042, RT-095 | La correa deja de ser un radio circular duro: pasa a ser una zona con forma por posición, tamaño escalado por el atributo y salida penalizada en vez de prohibida | ADR 0028 | Aplicado en v0.9.1 |
| R-9 | RF-031b, tabla de razas §3.4 | La habilidad de los elfos pasa a ser solo "esquivan mejor las entradas": se retira la mitad de intercepción. No es un ajuste de valor, es que el canal `intercept` (base 250) no admite ningún valor legal de la escala de puntos porcentuales (mínimo 5 pp = 500) que no sea una inmunidad, y una habilidad gratis e irrenunciable de plantilla entera no puede valer eso: medida, valía +6,6 puntos de tasa agrupada ella sola (D-29, D-30) | ADR 0026 §"Presupuesto de impacto" | **Pendiente de confirmación del revisor.** Reversible: el canal `interceptEvasion` sigue abierto en el motor y sin usar en `/data`; la mitad de intercepción vuelve el día que D-30 suba la base de `intercept` o pase la escala a múltiplos del canal |
| R-18 | RF-005, RF-020 | La plantilla inicial del club baja de 10 (7+3) a 9 (7+2, sin MID suplente); la capacidad base se queda en 10, así que hay un hueco libre en el primer mercado sin vender ni descartar | ADR 0079 (decisión del revisor) | Aplicado en `requisitos.md`; pendiente de subir la versión |
| R-19 | RF-114 (nuevo RF-114l) | El mercado garantiza exactamente un portero entre sus fichajes de pago (mínimo y máximo uno); el resto de fichajes, canteranos y el mercenario son siempre de campo | ADR 0080 (decisión del revisor) | Aplicado en `requisitos.md`; pendiente de subir la versión |

## Hallazgos de la revisión visual de la pantalla de Equipo (5 sep 2026)

| Id | Qué | Dónde |
|---|---|---|
| V-1 | La descripción generada encadena tres segmentos con dos puntos: *"Sangre caliente: al empezar el partido: el portador: sus entradas dejan al rival derribado más tiempo"*. Incumple `estilo-descripciones.md` (una frase). Hay que revisar las plantillas de composición en `data/l10n/*/templates.json`: el disparador y el objetivo deberían integrarse en la frase, no anteponerse | `data/l10n`, `DescriptionGenerator` | **Resuelta 2026-09-04**: el `layout` compone con comas en vez de dos puntos (`"{trigger}, {effects}."`) y `DescriptionGenerator.Describe` pone mayúscula inicial y punto final sobre el resultado ya compuesto. Los efectos que anteponían `{target}:` (`modifyProbability*`, `immunity`, `modifyKnockdownTicks*`) se reescriben como cláusula con verbo ("el portador suma +20% a su probabilidad de robar", "el portador deja al rival derribado más tiempo con sus entradas"), sin dos puntos en ningún punto de la frase. De paso, tres fallos encontrados al repasar el catálogo completo (`--describe es/en`): `addCounter` se ocultaba como bookkeeping duplicado de la escalada por contador y ahora no se describe; siete perks de acumulación filtraban su contador interno en `camelCase` (`battleReaderMatches`, etc.) sin traducción, ahora mapeado a un sustantivo (`partido`, `parada`...); y la condición `teammatesWithTagGt` producía "en el equipo de el portador" (concatenación `de el` inválida en español), corregida junto con `levelLt..Ne`, `attrLt..Ne` y `adjacentCountLt..Ne`, que tenían el mismo defecto sin estar aún ejercitadas por ningún perk. hot_blooded pasa de *"Sangre caliente: al empezar el partido: el portador: sus entradas dejan al rival derribado más tiempo"* a *"Al empezar el partido, el portador deja al rival derribado más tiempo con sus entradas."* (el nombre del perk lo antepone la UI de `/Game`, fuera de este encargo). Tests: `Sim.Tests/Perks/DescriptionTests.cs` |
| V-2 | La zona de acción de un centrocampista cubre casi media anchura del campo (columnas 0-10 desde la 5). Puede ser demasiado y explicar en parte por qué el marcaje y las entradas caen. Contrastar con `actionZone.shape` en el reajuste | `data/sim/tuning.json` |

## Abierto tras cerrar las costuras entre paquetes (`fase2-diseno.md` §16)

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| Z-A | **RESUELTA (ADR 0050 P1, `fase2-diseno.md` §26).** `EffectJson` valida ya contra `Sim.Perks.ProbabilityScale`: objetos, consumibles y perks escriben la misma unidad y la misma escala. *(Enunciado original)* **`data/items` y `data/consumables` no pasan por la escala por canal de la ADR 0035.** `Sim/Run/Systems/EffectJson.cs` (paquete X) lee el `value` de un efecto de objeto o consumible **directamente en base 10.000**, mientras que un perk lo escribe en puntos porcentuales y el cargador multiplica por 100. Son dos unidades para el mismo campo del mismo tipo (`EffectDefinition`) | Se deja como está en este encargo: unificar exige tocar `ItemLoader`/`ConsumableLoader` y reescribir los valores, y la ADR habla de perks. Hoy los dos únicos valores de probabilidad de `data/items` son múltiplos legales del escalón × 100 por casualidad, no por comprobación. **Abierta**: unificar la unidad y extender la validación, o declarar explícitamente que los objetos van en base 10.000 |
| Z-B | **Sigue abierta y cambia de naturaleza (ADR 0050 P1)**: con cuotas, `interceptEvasion` admite cualquiera de los catorce valores de la escala (ADR 0058: el techo lo pone la rareza, no el canal), así que ya no hay ningún impedimento de escala; solo queda revalidar la puerta de razas. *(Enunciado original)* **La mitad de intercepción de la habilidad racial élfica puede volver.** R-9 la retiró porque el canal `intercept` (base 250) no admitía ningún valor legal de la escala única que no fuese una inmunidad, y la dejó condicionada a "que D-30 suba la base o la escala pase a múltiplos del canal". La ADR 0035 hace exactamente lo segundo: `interceptEvasion` tiene ahora escalón 1 y admite +1 | No se repone en este encargo: cambia la habilidad de una raza y exige revalidar la puerta de razas (`RaceBalanceTests`). **Abierta**, y ahora es un ajuste de valor, no un imposible |
| Z-C | **RF-070 pide al menos 15 perks que acumulen efecto entre partidos y el catálogo tiene 6.** Tras §16.5 los seis acumulan de verdad **entre** partidos (antes solo dos lo hacían), que era el defecto grave | **Cerrada 2026-09-05 (paquete Z, Z-26)**: el catálogo tiene **quince**. Ocho nuevos repartidos por canal, por rareza y por tipo de RF-069, todos con `limit` de una activación por partido. Medido cuánto tarda un acumulador en llegar al tope: 3,3 contadores en la plantilla al jefe del acto 1, 12,1 al del 2 y 30,8 al final, es decir ~5 por perk justo al terminar la run (Z-27) |
| Z-D | **La fila del acto 1 de la ADR 0033 no cabe con holgura, y `butterfingers` la estrecha más.** La tabla pide 25 puntos de escalera entre el suelo de "correcta" y el techo de "muy buena"; el catálogo da 13 contra ese jefe, porque su modificador apaga los dos perks de portero en los cuatro escalones y uno de ellos era el diferenciador principal del escalón superior | Se documenta en vez de forzarlo (§16.6, Z-16). **Abierta**: o el catálogo gana perks que separen "buena" de "muy buena" sin depender del canal que el jefe apaga, o el jefe del acto 1 cambia de modificador, o la fila de la ADR se revisa con un ADR nuevo |
| Z-E | **El error de medida de una celda de la puerta es de unos 4 puntos y cuatro celdas quedan a menos de 2 de su borde.** Lo domina la varianza de generación de plantillas: al equipar, las celdas por raza se mueven ±10 puntos y el agregado de las cinco apenas (§16.6, Z-6) | Se acepta con la tolerancia declarada de ±2,5 (Y-9). **Abierta**: subir la muestra hasta que el error baje de 2 puntos cuesta más de tres minutos por lote, muy por encima de lo que puede costar una puerta |

| R-11 | RF-077, RF-078 | El arquetipo **restringido** deja de ser "solo funciona sobre portadores con una etiqueta" y pasa a ser un **objeto exclusivo de raza**: sin rareza, valores fijos y buenos, y solo aparece en runs de esa raza (3-4 por raza sobre más de 50 universales). Sigue cumpliendo la letra de RF-077 porque exige la etiqueta de especie para funcionar. RF-078 ("el equipamiento tiene rareza propia") admite ahora una excepción | ADR 0036 |
| R-10 | RF-077 | El arquetipo **frágil** deja de destruirse "tras N usos o si el portador se lesiona" (determinista) y pasa a tener una **probabilidad de rotura por partido**, visible antes de equipar y resuelta al terminar el partido. El **maldito** fija su magnitud en el doble de la de su rareza, en lo que sube y en lo que baja | ADR 0036 |

| R-12 | RF-023, RF-127, RF-128 | Las rarezas generables pasan a tres (común, poco común, raro). Los legendarios son personajes únicos, tres por raza, que se desbloquean al ganar una división y aparecen en la plantilla inicial de la siguiente. **RF-127 cambia**: la progresión meta sí otorga poder, compensado porque la división sube más la dificultad de lo que aporta el legendario | ADR 0039 |

| R-13 | RF-071 | **Se puede rechazar la recompensa.** RF-071 obliga hoy a elegir una de las tres opciones tras cada partido ganado; pasa a ser posible irse con las manos vacías (decisión `DeclineReward`). Con perks irreversibles (RF-072) y slots limitados por rareza (RF-023), quedarse con el menos malo **empeora** la build: ocupa el slot que necesitará el perk que llegue después. Y RF-071 se lee además con la ADR 0043: el número de elecciones ya no es una por partido ganado, es **una por nodo de liga o de élite y dos por nodo de jefe**, en `data/economy/economy.json` (`nodeRewards.picks`) | ADR 0043 | **Aplicado en código y datos; pendiente de subir `requisitos.md` de versión.** Medido: la política contextual rechaza 0,43 recompensas por run (el 4,2% de las elecciones) y la gastadora ninguna (`fase2-diseno.md` §19.3) |

## Abierto tras el paquete Z (runs completas y ajuste de economía)

Medidas y desglose en `docs/balance/fase2-resultados.md`.

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| Z-F | **La build llega tarde a la primera puerta.** Al jefe del acto 1 el once lleva **4,3 perks** contra los 14 de `*_correct`, y pasa el 47,6% (la ADR 0033 pide 45-60 para "correcta" y 60-75 para "buena", y su texto pide **buena** antes del primer jefe). El hueco es aritmético: 6 nodos de partido, 3 mercados, ~4 victorias (2,6 recompensas de perk) y una plantilla inicial que entra con 1 o 2 perks porque un común entra con cero (RF-023) | La única salida que no rompe la escasez de la ADR 0037 es que **el club inicial traiga una build**, y eso toca RF-023/RF-005. **Abierta: exige un ADR.** Las alternativas empeoran otra cosa: más partidos en el acto 1 choca con RF-003b, más de una recompensa por victoria con RF-071, y abaratar los perks con la ADR 0037 |
| Z-G | **La banda "tasa de victoria de la run 25-40%" de `fase2-diseno.md` §10 no es compatible con la tabla de la ADR 0033.** El producto de las tres celdas "muy buena" es **29,5%**, y la trayectoria que la propia ADR describe (buena -> buena/muy buena -> muy buena) da **21,8-28,2%**. El techo de 40% es inalcanzable aunque se juegue perfecto y el punto medio (32,5%) exige build muy buena en las tres puertas, que es lo contrario de lo que la ADR dice | La banda coherente es **20-30%**. Medido hoy: 13,0%. **Abierta: cambiar el rango exige un ADR** (RT-057) |
| Z-H | **CERRADA, y llevaba tiempo resuelta.** El bloqueo era que un objeto equipado no valía casi nada con el formato antiguo. La ADR 0036 (objetos como bonos de atributo por rareza) ya está implementada en `data/items/*.json` (verificado, ej. `duelists_gloves.json`: `attributeBonus`) y `ItemScale.ValueOf` calcula sobre ese modelo. La medida original (contextual empata con la ahorradora, +0,8) es de antes de esa reescritura | **CERRADA.** Medido de nuevo el 2026-09-09 con el lote de cierre de AT-A (1.200 runs, gate de mercado activo, ADR 0086): `runWinRate` contextual **20,83** contra ahorradora **18,42** (+2,41) y gastadora **11,92** (+8,91) — las tres doctrinas están claramente diferenciadas, no hay empate. `leftoverGoldShare_saver` 17,14 > `leftoverGoldShare` contextual 12,93, la otra mitad del criterio de la ADR 0037, también se cumple |
| Z-I | **Las lesiones han desaparecido del bucle de run.** Medido: **0,04 lesiones propias por partido**, contra las 0,62 por partido (los dos equipos) que RT-056 mide en el lote de fase 1. La fórmula de `tuning.injury` mide los dos términos **contra 50**, que es el nivel 1: la progresión suma 2 por nivel (RF-027), así que un titular de nivel 6 resta 60 puntos básicos sobre una base de 40 y la entrada limpia se acota a cero. Un equipo que sube de nivel es **inmune a las lesiones** | Consecuencias medidas: 0,21 lesiones graves por run, **cero tratamientos de clínica** en 500 runs y **cero muertes** (banda 0,5-2). **No se ha tocado `data/sim/tuning.json`**: es global y recalibraría RT-056 y las puertas de fase 1 y 2 a la vez. La corrección es hacer la fórmula **relativa** —al que entra contra su víctima, no contra 50— y es un reajuste con su propio ADR. **Abierta** |
| Z-J | **Dos de los cuatro sumideros de RF-114k son contenido muerto.** Uso real por run: mercado 674 de oro, clínica **0**, rerolls 2,8, salarios **0**. RF-114k se cumple como está escrito (el oro de un acto paga 2,40 sumideros y nunca los cuatro) pero en la práctica solo se usa uno | La clínica no se usa por Z-I; los mercenarios no se usan porque con trece jugadores nunca faltan cuerpos. **Abierta**: o el desgaste vuelve (Z-I) o los dos sumideros necesitan otra razón para existir |
| Z-K | **Dos métricas de escasez de la ADR 0037 se oponen entre sí.** Bajar la fracción asequible del surtido (40,5%, banda 20-35) exige subir precios o bajar oro, y las dos cosas suben las visitas en blanco (49,2% de las runs, banda 10-25). Con el catálogo actual no hay configuración que cumpla las dos | Lo que las acerca es la dispersión de precio dentro de la rareza, ya aplicada (`market.priceSpreadPercent` 70, Z-24). Además, la métrica de visitas en blanco se cuenta **por run** y una run visita 3-9 mercados: **por visita es el 9%**, dentro de lo razonable. **Abierta**: o la métrica pasa a contarse por visita, o la banda por run sube a 35-60% |
| Z-L | **CERRADA, y llevaba tiempo resuelta.** El oro sobrante (23,2%, techo 15%) era el precio de que el once se saturase sin que el equipamiento absorbiera nada. Con la ADR 0036 aplicada (Z-H) el objeto vuelve a ser un sumidero real | **CERRADA.** Medido de nuevo el 2026-09-09 con el lote de cierre de AT-A (1.200 runs, seed 1): `leftoverGoldShare` **12,93%**, dentro de la banda ≤15% que esta fila esperaba de la ADR 0036 |

## Abierto tras el paquete AB (curva por actos, ADR 0043 y 0044)

Medidas y desglose en `docs/fase2-diseno.md` §19.

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AB-A | **Las muertes por run siguen fuera de banda** (0,06 contra 0,5-2) y ahora se sabe que el desgaste no las produce. Sensibilidad medida: con el multiplicador por acto en 200/400/620 salen **0,11** muertes y con 300/650/950 salen **0,30**, y ese segundo punto ya produce 1,29 lesiones por partido entre los dos equipos, el doble del techo de RT-056 para un partido suelto. La causa es que la única vía viva de RF-093 —alinear a un grave sin tratar— es una **decisión** que una política razonable no toma: con trece jugadores nunca faltan siete sanos y, cuando falta uno, el oro cubre la clínica | Confirmado al revés: la doctrina **gastadora**, que se queda sin oro para tratar, muere el triple (0,19 por run). **Abierta**: la banda necesita la **segunda vía de RF-093** (perks rivales letales, hoy inexistentes en `/data`) o una plantilla más corta. No es un ajuste de datos |
| AB-B | **La ventaja de 8 puntos de la ADR 0037 es ahora estructuralmente más difícil.** Con el trampolín, la run cobra 9,8 elecciones gratuitas y compra ~6 piezas: lo único en lo que las tres doctrinas se diferencian —el mercado— ha pasado a ser el 40% de la build, y la mitad de eso son objetos. Medido: contextual 23,6%, ahorradora 24,2%, gastadora 16,6% | Lo que separa está identificado y es la **colocación**, no el gasto: la gastadora compra más (1,53 por mercado), termina con más perks en el once (10,3 frente a 8,9) y gana 7 puntos menos. **Abierta**: o la ADR 0037 acepta que su métrica es la distancia contextual-gastadora (+7,0), o hace falta una palanca de compra que la ahorradora no pueda replicar (la **transferencia** de objetos, RF-076b, que la política sigue sin usar) |
| AB-C | **Una run perdida cuesta el 61% de una ganada** (21,3 nodos y 12,0 partidos frente a 35 y 20), no el tercio que `curva-de-dificultad.md` §2.2 toma del género | Es **incompatible por construcción** con la ADR 0043, que pide que la mayoría de las derrotas caigan en el acto 2 —a dos tercios del recorrido—. Gana la ADR, que es posterior y es directriz. En minutos absolutos (25-30 frente a 45-55) sí se parece a la referencia. **Abierta solo como constancia**: si el revisor quiere el tercio, hay que mover las derrotas al acto 1 y eso deshace el taller |
| AB-D | **Una celda de la curva pasa por tolerancia**: *correcta* contra el jefe final mide 28,8 con techo 28 (±2,5). Medido: esa celda **no depende de la densidad de la build** (con 5 perks y sin contadores pasa el 29,2% y con 7 perks y contadores el 29,1%), así que solo la mueve el jefe, y bajarlo hunde *buena* y *muy buena*, que están en el suelo de su banda | Mejor que el estado anterior (tres celdas por tolerancia, AA-18). **Abierta**: separar esa fila exige material nuevo en `data/balance/builds/`, no calibración |
| AB-E | **RF-114k se cumple en el 99,91% de los actos, no en el 100%.** `actsWithAllFourSinksAffordable` mide 0,09% con 500 runs (cero en el lote de 60 de la puerta, que es el que se afirma). Un acto 3 muy rico con solo dos mercados llega a pagar los cuatro sumideros | La aritmética entera de la ADR 0044 deja menos margen que la escala vieja (el sumidero de mercado era 1,8 veces el oro del acto y ahora es 1,1). **Abierta**: o se sube el precio de los comunes por encima de la banda de la ADR 0044, o la puerta afirma "< 0,5% de los actos" en vez de "ninguno" |
| AB-F | **La escasez se cuenta por run y la escala nueva la empeora**: "llegar a un mercado sin poder comprar nada" pasa del 45% al 78% de las runs, y **por visita sigue siendo el 15%**. Con precios de 6 a 47 y un acto que gana 23, quedarse con 4 de oro es habitual | Es el mismo Z-K de siempre, ahora con más filo: la métrica está contada por run y una run visita nueve mercados. **Abierta**: contarla por visita, o subir la banda por run a 60-85% |
| AB-G | **La descripción de un efecto con varios objetivos concuerda en singular**: *"el compañero de su izquierda y compañero de su derecha suma +15%..."* (`pivot_duo`, `wing_overlap`, `diagonal_press`) | Es de la misma familia que V-1 y no lo toca este paquete: hay que componer la lista de objetivos con concordancia (y repetir el artículo) en `DescriptionGenerator`. **Abierta** |
| AB-H | **La plantilla `modifyProbabilityPaired` no la usa ningún perk.** Tras la corrección de §16.4 el tratamiento por par solo existe en el canal `pass`, y los seis perks con efecto sobre el vinculado usan `intercept`, `tackle`, `dribble` o `shotOnTarget` | Se conserva porque la mecánica existe (ADR 0021) y es el ejemplo con el que la propia ADR la introduce. **Abierta**: o el catálogo gana un perk de pase por par —que es el caso que la ADR describe— o la plantilla y la rama del motor se retiran |

| R-14 | RF-020, RF-011 | La plantilla pasa de "mínimo 7, máximo 12" a **base 10 ampliable a 12** mediante un **nodo de inscripción** de coste creciente, que es una decisión de ruta. Se añade ese tipo de nodo a RF-011 | ADR 0046 |

| R-15 | RF-128 | Las divisiones añaden como palanca el **oro inicial decreciente** (10 · 8 · 5 · 2) junto al encarecimiento del mercado, además de las reglas que ya listaba la tabla | ADR 0044 |

| R-16 | RF-093, §8 | **Un jugador sano puede morir**: se retira la regla "un jugador en estado sano nunca puede morir" y con ella la mitigación del riesgo "muerte percibida como injusta". La sustituyen las cinco condiciones de la ADR 0048 (anticipación, evitación, reducción del riesgo, recuperación y rareza) | ADR 0048 |

| R-17 | RF-071 | La recompensa de un partido de liga pasa de **1 de 3** a **1 de 2**; élite mantiene 1 de 3 con rareza mejorada y el jefe da 2 de 3. Mueve peso de las recompensas gratis al mercado | ADR 0049 |

## Abierto tras el paquete AD (morir estando sano y 1 de 2, ADR 0048 y 0049)

Medidas y desglose en `docs/fase2-diseno.md` §21.

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AD-A | **La condición 3 de la ADR 0048 se cumple a medias: el indicador de riesgo tiene rango, pero la mitad útil es pequeña.** Dos políticas idénticas salvo que una atiende al indicador al alinear mueren **1,51 y 1,57** por run (500 runs por doctrina): 3,8 puntos porcentuales, dentro del ruido. El barrido de `--risk-aversion` (300 runs) dice por qué: exponer al eslabón débil a propósito cuesta **+20%** (1,90 contra 1,58) y protegerlo solo ahorra **−3%** (1,54 contra 1,58). Lo que sí se separa es la **tasa de victoria** (21,0 leyendo contra 17,6 sin leer): la contrajugada paga en partidos, no en cuerpos | La causa está medida: **una alineación elegida por valor deportivo ya está cerca de la más segura**, porque el valor de un jugador incluye su aguante. La ADR 0048 dice que si no se separan "la decisión hay que revisarla". **Abierta**: o la condición 3 se reformula (el indicador sirve para no equivocarse, no para protegerse), o hace falta una palanca de alineación que el valor deportivo no capture ya —la más obvia es que la formación deje de ser un 2-3-1 fijo (RF-041), porque hoy siete casillas obligan a exponer a siete jugadores— |
| AD-B | **La condición 2 (evitación) se cumple en el 50% de los nodos letales, no en el 100%.** Con cuatro rivales letales de cinco por acto era el 30%; bajar a tres lo sube al 50%; bajar a dos lo subiría al 77% pero deja las muertes en 0,82 por run, fuera de la banda 1,5-3 | El 100% no es alcanzable con tres de cinco: `MapGenerator` reparte los rivales con un cursor consecutivo sobre la baraja del acto y tres marcados en un ciclo de cinco tienen por fuerza dos adyacentes. **Abierta**: cerrarla exige **dos** letales por acto y la restricción "dos letales nunca en la misma capa" en el generador, y cuesta la banda de muertes; hoy la cota de no regresión está en el 45% (`LethalRiskTests`) |
| AD-C | **La ADR 0048 pide dos cosas incompatibles sobre la rareza de la muerte**: banda de **1,5-3 muertes por run** y *"una muerte por perk letal cada dos o tres runs"*, que son 0,3-0,5. Un factor de cinco. Y no hay margen: `deathsAct1` mide **0,00**, así que la vía 1 de RF-093 no aporta nada y las 1,53 muertes son **todas** de perk letal | Se aplica la banda, que es la que la ADR fija explícitamente y la que mide la puerta. **Abierta**: si lo que el revisor quiere es la frase, la banda baja a 0,3-0,5 y `lethalChance` con ella (es un único número por perk en `/data`) |
| AD-D | **La apuesta de la ADR 0049 sobre las doctrinas ha ido al revés**: con menos elecciones gratis, `contextualAdvantage` cae de **+5,6 a +0,2** en vez de subir hacia los 8 puntos de la ADR 0037. Contextual 21,0, ahorradora 20,8, gastadora 14,8 | La causa que los datos sugieren: la ventaja de la contextual no estaba en comprar sino en **tener con qué elegir**; con la build más pobre y el surtido igual de caro, su presupuesto casi nunca alcanza al raro y su regla degenera en la de la ahorradora. La distancia con la **gastadora** sí sobrevive (−5,4). **Abierta**: es el hallazgo que más pesa en contra de la ADR 0049 y la palanca que queda es la de siempre —precios—, que choca con `brokeMarketRunShare` (Z-K) |
| AD-E | **La escasez no se ha corregido sola**, que era la hipótesis explícita del encargo: `affordableShareAtMarket` pasa de 59,3 a **56,7** con objetivo 20-35, y la mayor parte de esos dos puntos y medio los pone el surtido ampliado (`perkOffers`/`itemOffers` de 4 a 5), no las muertes | Con más muertes el oro por run **baja** (56,2 → 52,5) y con él el gasto en mercado (37,6 → 35,2): el jugador compra menos de un surtido mayor, pero la fracción asequible no cambia de orden. **Abierta**: es el mismo Z-K/AB-F, y tocar precios sigue enfrentando esta métrica con `brokeMarketRunShare` |
| AD-F | **`sinksAffordablePerAct` se salió de su banda 2-3 y ha habido que pagar por devolverlo**: con el oro por acto más bajo medía **1,96** con la muestra de 60 runs de la puerta (2,08 antes del paquete). Se arregla bajando `clinicCost` de 10 a **8**, y queda en 2,12 | Ninguna otra palanca servía: el sumidero de mercado cuesta ~36 de oro contra los 22-29 que gana un acto, así que abaratar los comunes lo suficiente habría disparado `affordableShareAtMarket`. La bajada tiene además razón de diseño (con 1,5 muertes por run, curar tiene que estar más al alcance), pero el uso de la clínica **baja** igualmente (4,7 → 3,7 de oro por run): lo que mata ahora es el perk rival y contra eso la clínica no cura. **Abierta como vigilancia**: es una puerta dura y cualquier cosa que baje el oro por acto vuelve a sacarla |

## Abierto tras el arreglo de nombres de club y rival (RF-004, RF-015)

`data/clubs/` es nuevo (uno por raza de lanzamiento) y `data/rivals/*.json` gana `description`. Cambio
cosmético: no toca `/data/perks`, `/data/economy` ni `data/sim/tuning.json`, y no mueve ninguna métrica de
`/Balance` (487 tests en verde, sin tocar `/Sim` fuera de dos correcciones de nombre visible). Detalle en
el informe del encargo.

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AE-A | **RF-004 pide una "regla especial" por club; hoy es, en la práctica, la regla de su raza.** `ClubDefinition.SpecialRule` no inventa una mecánica nueva de club: es el id del perk de habilidad racial que ya existe (ADR 0026) | Es la lectura explícitamente pedida en el encargo ("no la inventes... apunta a la habilidad racial que ya existe"). **Abierta**: si algún día un club quiere una regla propia distinta de su raza, hace falta diseñarla y este campo ya está preparado para llevarla (basta con que apunte a otro perk, o quede vacío) |
| AE-B | **`ClubDefinition.StartingGold` y `.Roster` son informativos, no la fuente real.** El oro de partida de la run lo sigue calculando `StandardRunSystems.NewRunSetup` desde `economy.startingGoldByDivision` (hoy 10 para todos, división Tercera) y la plantilla real la sigue generando `TeamGenerator` vía `RunSetup.GeneratedQuality`; ninguno de los dos lee `data/clubs/` | Decisión explícita para no mover ninguna métrica de balance en un arreglo cosmético (el encargo lo pedía expresamente). **Abierta**: enchufar la plantilla y el oro reales de cada club es trabajo de balance con su propio lote de `/Balance`, no de esta corrección |
| AE-C | **El nombre visible del equipo propio y del rival venía del id de datos, no de su nombre.** `RunEngine.BuildMatch` etiquetaba el equipo propio con `state.ClubId` ("underleague_fc") y `RivalTeamBuilder.Build` etiquetaba al rival con `team.Id` ("act1_elf_swiftwing"): los dos llegaban tal cual al marcador del partido, al log de eventos (RF-121) y al informe post-partido (RF-119) | Corregido: `RivalTeamBuilder.Build` usa `team.Name.Es`, y `StandardRunSystems.TransformMatch` reescribe el nombre del equipo propio con `ClubCatalog.Find(state.ClubId)?.Name.Es` cuando lo conoce. El idioma queda fijo a "es" en los dos sitios, igual que el resto de textos que `/Sim` genera fuera del catálogo de localización, hasta que la fase 4 elija idioma en tiempo de ejecución (RT-073) |

## Abierto tras el mapa de cuatro carriles (ADR 0053, `fase2-diseno.md` §24)

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AH-A | **RF-011b tiene dos mitades y con capas de mercado mixtas ya no se cumplen igual.** La garantía dura ("un mercado alcanzable en dos saltos desde cualquier punto") se sigue cumpliendo por construcción y se comprueba sobre 1.000 mapas; la densidad ("un nodo de mercado cada 3-4 nodos") pasa a cumplirse **por exceso en lo que el mapa ofrece** —una capa de mercado cada 2— y a **no cumplirse en lo que el jugador recorre**: el 99% de los actos admiten un camino que no pisa ningún mercado | Lectura aplicada: la mitad que manda es la de los dos saltos, porque es la que RF-011b justifica ("es lo que convierte jugar en inferioridad en una decisión y no en una trampa") y la densidad era el mecanismo con el que W-2 la conseguía. **Abierta**: si al subir `requisitos.md` de versión se quiere dejarlo escrito, el texto de RF-011b debería hablar de mercados **ofrecidos** y no de mercados por nodo recorrido |
| AH-B | **El suelo de partidos de una run baja de 20 a 17** (`fase2-diseno.md` §10 pide 18-22). Es consecuencia directa de la ADR: si el mercado se puede esquivar, el partido también | El mando es `MapGenerator.PorousMatchLayers` (hoy 1: una capa de partido por acto ofrece alternativa en la misma capa). **Abierta**: decidir si 17 es aceptable como suelo teórico —lo alcanza solo un camino que se desvíe siempre, y desviarse siempre significa no ganar oro— o si §10 debe recoger el rango nuevo |
| AH-C | **La política automática de `/Balance` impide, hoy, la capa con partido y mercado a la vez.** `RunPolicy.ChooseNode` puntúa el mercado con 90 y un partido de liga con 50 menos la dificultad: pesos calibrados cuando el mercado era un cuello de botella y no competía con nada. Con mercado y partido en la misma capa la política se desvía **siempre**, y como el presupuesto de RF-003b deja las capas de partido justo en el tope, eso es un partido menos por acto: medido, `matchesPerFullRun` = 17,0 y la puerta (18-22) en rojo | Resuelto **por ahora** apartando el mercado de las capas con partido (§24, AH-8b), así que la imagen que pedía la ADR 0053 —"partido, mercado o clínica" en la misma capa— se queda a medias: hoy es "partido o clínica" en una capa por acto y "mercado o clínica" en las de mercado. **Abierta**: revisar esos pesos es un paquete propio, porque cambiar el instrumento de medida obliga a volver a medir la economía entera; no se ha tocado aquí porque otros dos paquetes estaban midiendo con él |

## Abierto tras los perks multiplicativos sobre cuotas (ADR 0050 P1, `fase2-diseno.md` §26)

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AJ-A | **CERRADA (ADR 0058, `fase2-diseno.md` §27.4).** La descripción pasa a hablar de **cuota**: *"multiplica por 1,3 sus opciones de pasar"* / *"multiplies their passing odds by 1.3"*, que es la alternativa exacta que esta entrada dejaba sobre la mesa. Exacta en todos los canales, misma cifra para el positivo y su inverso ("divide por 1,3"), y enseña que dos perks se multiplican en vez de sumarse. *(Enunciado original)* La convención proporcional de la P1 decía "un 30% más de probabilidad de X" para `k = 1,3`: exacta cerca de 0 y falsa cerca de 1 (sobre `pass`, base 77%, el aumento real es del 5,6%) | Resuelta: `{value:odds}` rinde el factor decimal, `data/l10n/{es,en}/templates.json` y `docs/estilo-descripciones.md` reescritos, `ProbabilityScale.ToPercent` retirada |
| AJ-B | **CERRADA (ADR 0058, `fase2-diseno.md` §27.3).** Los rivales estáticos de `data/rivals/` pasan de 1-4 perks sin pendiente a **2 / 7 / 9** por acto, con build legible en su línea y con la **etiqueta de estilo** que les faltaba: `RivalTeamBuilder` escribía `StyleTag.Neutral` para todos y no la metía en `Tags`, así que los perks que consultan `hasTag(owner,'Bulwark')` o `teammatesWithTag(owner,'Fine')` no se activaban nunca y cobraban sus `elseEffects` —`act1_orc_ironclad` llevaba `bulwark_stance` en un portero Neutral y **se dividía por 2 la entrada de todo el equipo**—. *(Enunciado original)* Los rivales ordinarios de una run no llevan ningún perk | Resuelta como cambio, **no** como palanca: mueve el suelo sin build (12,08% → 10,08%) pero **no abre** el hueco entre perfiles, y §27.6 explica por qué no puede. Ver **AK-B** |
| AJ-C | **CERRADA (ADR 0062, §29.6).** El umbral de `buildsWinDifferently_passChain` pasa de 1,30 a **1,11**, derivado de lo que el canal de pase puede dar con la escala de cuotas: medido aislado, siete `fine_touch` comunes sobre el once alargan su propia cadena un **10,8%** y ni con el techo legendario ×6 pasan del **19,1%**; el 1,30 venía de la fórmula aditiva que clavaba el pase en el 98% y exigiría un ×14,6 que no existe en la escala. La puerta mide 1,233 y pasa. *(Enunciado original, ADR 0058 §27.9)* **MEDIO RESUELTA.** `badBuildsLoseToNone_elf_out_of_zone` **vuelve a banda sola**: la build mala vive de sus `elseEffects` y `forward_line` es poco común, así que con el techo por rareza su castigo pasa de ×0,5 a ×0,333. `buildsWinDifferently_passChain` sigue roja (1,19 contra ≥1,30) y seguirá: mide siete `fine_touch`, que es **común**, así que su techo sigue siendo ×2 y el umbral sigue calibrado contra un canal de pase saturado que la P1 hizo imposible por diseño | **Cerrada.** El ADR es el 0062 y la derivación queda escrita en `Sim.Analysis.BuildMetrics.MinPassChainRatio`. **Hay que volver a derivarla hacia arriba cuando AL-A se resuelva**: hoy el umbral lo acota la aritmética del canal, no el diseño de la build |
| AJ-D | **CERRADA (ADR 0072 §6).** Era frágil por tamaño de muestra: afirmaba que la ahorradora termina con más oro sin gastar que la contextual comparando dos medias sin margen sobre **60 runs**, y con 60 salía 13,29 frente a 14,18 (orden invertido) mientras con 1.200 salía 14,86 frente a 12,10 (orden correcto). La muestra de `FullRunGateTests` sube a **240 runs por doctrina** | **Cerrada.** No se tocó ninguna afirmación ni ninguna cota: se le dio muestra, que es la corrección que la propia entrada pedía. El mismo cambio salvó `deathsPerRun`, cuya cota de no regresión estaba a 2,7 desviaciones con 60 runs |
| AJ-E | **Se agrava y se relee como AK-A (ADR 0058, §27.7).** Con el acto 1 revertido a exactamente lo que era, las derrotas del acto 1 se quedan en el **30,9%**, por encima del 29,3% que el encargo fija como techo, y la causa **no** es la capa del rival sino el techo por rareza sobre los **catorce** perks del jefe frente a los cinco del jugador. *(Enunciado original)* Recalibrar el jefe del acto 1 le quita al acto 1 su carácter de taller: `grimhold_guns` sube de calidad 17 a 31 | **Abierta**: recalibrar el jefe cuadraría el número pero la ADR 0056 solo lo permite cuando una celda de la ADR 0033 se sale de banda, y las doce están dentro. O se acepta que el acto 1 muerde, o se revisa la fila del acto 1 de la ADR 0033 |
| AK-A | **CERRADA por la vía buena (ADR 0060, §28.11): `defeatShareAct1` vuelve a 29,74% sin recalibrar ningún jefe**, al bajar la capa de build del rival ordinario. *(Enunciado original)* **`defeatShareAct1` sube al 30,9% sin haber tocado el acto 1** (`fase2-diseno.md` §27.7). El techo por rareza de la ADR 0058 hace más fuerte a quien más perks lleva, y en la puerta del acto 1 eso es el jefe: catorce perks contra los cinco con los que llega el jugador. Medido aislado, solo el punto 1 ya sube la cuota (35,77% en la semilla 1 frente a 31,25% de esa misma semilla antes) | No se ha recalibrado el jefe: las doce celdas de la ADR 0033 están dentro de banda y la ADR 0056 solo autoriza tocar un jefe cuando su celda se sale (RT-057). **Abierta**, y es la misma conversación que AJ-E |
| AK-B | **DIAGNOSTICADA (ADR 0060, §28.3, ahora AL-A): el recorrido del catálogo no lo limita la rareza, lo limita la BASE DEL CANAL.** Medido: cuatro ×2 sobre `pass` (base 77%) compran 0,19 puntos de tasa de victoria y los mismos cuatro sobre `intercept` (base 2,5%) compran 15,54. Y el hueco **sí se abre** (6,27 → 10,03) con una palanca que no es de fuerza: el castigo del perk mal puesto. *(Enunciado original)* **Ni el techo por rareza ni la capa de build del rival abren el hueco entre perfiles** (`fase2-diseno.md` §27.5 y §27.6), que es el criterio con el que la propia ADR 0058 se declaraba falsificable. Dos causas medidas: (1) una build buena y una mediocre llevan casi la misma mezcla de rarezas —42,8/45,5/11,4/0,4 frente a 56,2/35,5/8,2/0,1—, así que el techo por rareza les da solo un 6,9% de diferencia de multiplicador medio, que a la sensibilidad medida (~1,3 puntos por doblado del peso logarítmico de la capa de perks) es una décima de punto; (2) la capa del rival es un solo número y baja a las dos builds a la vez hacia la parte plana de la sigmoide, donde la misma diferencia de fuerza vale menos puntos | **Abierta, y es la decisión de fondo**: los objetivos 1 y 2 de la ADR 0056 (buena al 60%, mediocre al 42-45%) son incompatibles con cualquier palanca que mueva a las dos builds en el mismo sentido. Hace falta una que aumente el **recorrido del catálogo** entre construir bien y construir regular |
| AK-C | **CERRADA (ADR 0060, `fase2-diseno.md` §28.1-§28.2).** `data/economy/perk-values.json` se regenera con **3.072 partidos por perk** (dos lotes de 48×32, semillas 5 y 11, sumados) en vez de 384. Y el hallazgo es que **el desafine era ruido, no sesgo**: a 384 partidos la desviación por fila es de 51 unidades y la dispersión real entre perks es de 50, así que la mitad de la varianza de la tabla vieja era error de medición. Con el instrumento afinado **las tablas no se mueven** (build buena 52,43 → 52,28 en el acto 2, hueco 6,27 → 6,81). Lo que sí entra son seis filas que no existían, entre ellas los tres maestros medibles, que la contextual puntuaba con un `?? 0` | Cerrada. La build mala **empeora** al afinar (9,92% → 12,17% de runs completadas): los pesos del pool cambian y la gastadora compra otra cosa |
| AL-A | **El recorrido de un perk lo fija la base de su canal, no su magnitud** (`fase2-diseno.md` §28.3). Con cuotas, `pass` (77%), `dribble` (72%) y `shotOnTarget` (78,5%) no pueden comprar tasa de victoria por mucho que se suba la magnitud —cuatro ×2 apilados sobre el mismo portador valen 0,19 / 0,08 / 0,73 puntos— mientras `intercept` (2,5%) compra 15,54 con los mismos cuatro. La P1 igualó lo que un perk vale en **cuota**, no lo que vale en **tasa de victoria** | **Abierta, y es la decisión de fondo que hereda a AK-B.** Es una corrección de fundamentos: mover perks a canales con recorrido, o mover la base de los canales altos (lo segundo toca RT-056 y la sensación de fútbol). No cabía en este paquete |
| AL-B | **FALSIFICADA POR MEDICIÓN (ADR 0061, `fase2-diseno.md` §29).** Bajar el peso de los atributos frente a la build **no sirve a los tres objetivos a la vez**: es el mismo número que la fuerza del rival. Los atributos entran en el motor como una **diferencia**, así que la curva de nivel, el presupuesto de generación y los factores del motor son la misma operación sobre el mismo término y mueven a las dos builds a la vez. Medido en siete condiciones de 600 runs, **el hueco se queda entre 9,7 y 10,4** mientras la tasa de victoria se mueve veinte puntos. Y el experimento compensado lo cierra: `attributesPerLevel` 1 con los rivales bajados 1/5/4 devuelve los partidos ordinarios a su sitio (buena 58,12, hueco 10,82) y hunde el suelo a 5,17 **rompiendo** `deathsPerRun` (1,19), las derrotas del acto 1 (37,39) y la run (10,33). *(Enunciado original)* **El 60% de la build buena no es alcanzable con la capa de build del rival ordinario** (§28.9) | **Cerrada como palanca; el objetivo sigue abierto.** El tipo de cambio queda medido: un punto de atributo sobre la plantilla = **+1,33** puntos de tasa de victoria del acto 2 = **1,4 perks**; la build entera vale +6,6 y la capa de nivel +13,6. Los objetivos 1 y 2 de la ADR 0056 piden un hueco de 16,5 y la única palanca viva es **AL-A** |
| AL-C | **La build mediocre concentra MÁS en su línea que la buena** (§28.4): 4,32 perks distintos de su mejor línea frente a 3,17, y 0,44 maestros por run frente a 0,22. La contextual **rechaza** perks (`WorthASlot`) y termina con uno y medio menos; su filtro es el valor medido, que es ciego a la línea | Falsifica el punto 3 de la ADR 0059 (pago por coherencia): pagaría más a quien construye peor. **Cerrada como diagnóstico**; queda abierto si el modelo de "build buena" debería incluir una doctrina que persiga una línea a propósito, porque hoy no existe |
| AL-D | **`randomBuildNearNone` es el techo de la palanca de castigo** (§28.6). Con `flank_specialist` a `team dribble -200` la métrica cayó a **38,54** (banda 40-60) y hubo que **bajar la palanca**, no el umbral. El otro techo es la celda `incoherente` del jefe del acto 1: 21,6 sobre un mínimo de 20 | **Abierta**: si se quiere apretar más la incoherencia hay que hacerlo por una vía que no toque una build de perks al azar. `human_random` lleva dos perks mal puestos de ocho |
| AL-E | **CERRADA.** `Sim/Analysis/MatchMetrics.cs` ya no lleva el `65..80` escrito a mano: decide con sus propias constantes `BetterTeamWinRateMin`/`Max`, que son los 70-88 de la **ADR 0054**. Verificado en el lote de referencia de 1.000 partidos: `betterTeamWinRate_human_60_vs_human_40` = **79,52**, IN | Cerrada por el revisor antes de este paquete |

## Abierto tras medir el peso de los atributos (ADR 0061 y ADR 0062, `fase2-diseno.md` §29)

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AM-A | **DIAGNOSTICADA DEL TODO (ADR 0063, §30.2 y §30.10): el −1,8 es castigo entero y devuelve una elección de diseño, no una calibración.** Medido con el catálogo **sin `elseEffects`** (1.200 runs por doctrina): los perks de la build mediocre valen **+3,55** cuando no pueden castigar y **−1,85** cuando sí; el castigo le cuesta **−5,40**. Los de la buena no se enteran (+6,64 con castigos, +6,14 sin ellos). Y el **60% del hueco es castigo** (10,03 → 4,01 al quitarlo): sin castigo, sostener 9,8 pediría un premio 3,2 veces mayor y dejaría a la build buena en el **71%** del acto 2. Perk a perk, cuatro perks son el 83% del castigo del catálogo (`spearpoint` 179 unidades él solo, el 51%) y **no hay perks que valgan negativo estando bien puestos**: los 16 que miden negativo sin castigo están entre −2,2 y −0,3 puntos contra una desviación de fila de 1,15, y los cuatro que llegan a dos desviaciones se explican por AL-A (canal sin recorrido), por acumulación medida en un solo partido (AN-B) o por letalidad. *(Enunciado original, §29.5)* Con `rewardPerkWeight = 0` la doctrina gastadora **mejora**: 47,94 → 49,79 en el acto 2 y el hueco cae de 10,03 a 1,54 | **La elección es del revisor y está en la ADR 0063.** *Comprar siempre mejor que no comprar* es `V_mediocre > 0`, y eso **fija a la build mediocre en 49,8 o por encima**, porque 49,79 es lo que gana esa misma doctrina **sin** perks; el objetivo 2 de la ADR 0056 la quiere en 42-45. Los dos no pueden ser verdad a la vez: "mediocre al 42-45%" *es* la frase "construir mal es peor que no construir". Tres salidas, ninguna es calibración: (1) revisar el objetivo 2 y subir el premio un 24% —lo que pide AN-A—, (2) mantener el objetivo 2 y aceptar que comprar mal salga a deber, que es lo que hay hoy, o (3) bajar el suelo sin build por la vía de **qué cuesta perder un partido ordinario** (RF-002c), que nadie ha medido |
| AM-B | **`generation.budgetByRarity` y `generation.budgetPerLevel` no son mandos del jugador**: `PlayerGenerator` los usa para todo el que se genera, y los **tres jefes se generan** (`quality 31, level 8, rare`). Subir el presupuesto sube también al jefe; los rivales ordinarios no se enteran porque sus atributos están escritos a mano en `data/rivals/`. El único mando que toca sólo al jugador es `progression.attributesPerLevel` | **Anotado, no abierto.** Costó una tanda de medición entenderlo: la primera sonda daba +6,7 puntos de partido ordinario y **menos** runs completadas, que no tenía sentido hasta ver de dónde venía. Tenerlo delante antes de usar el presupuesto de generación como palanca de balance |

## Abierto tras medir el ámbito del premio (ADR 0063, `fase2-diseno.md` §30)

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AN-A | **El premio del catálogo lo cobra también la oposición; el castigo no** (§30.7). La ADR 0060 §28.10 ya midió que ningún jefe paga el castigo, porque los tres tienen sus perks bien puestos; la otra mitad es que **todos cobran el premio y lo cobran con dos y tres veces más perks que el jugador**: `grimhold_guns` lleva **14** slots de perk, `the_hunt` 14 y `eternal_crown` **27**, contra los **3,2 / 6,3 / 8,6** con los que el jugador llega a cada puerta (`groups.json`, `actDensity`). Medido: mover a `team` el premio de los ocho perks cuyo castigo ya era de equipo **empeora** a la build buena (57,53 → 56,28), hunde `deathsPerRun` a **1,04** y sube las derrotas del acto 1 a **43,98** | **Abierta.** Subir el premio es la única forma de llegar a `V_buena ≥ 8,26`, que es lo que exige "comprar > no comprar" con hueco ≥ 9,8; pero exige **recalibrar a la vez** `data/rivals/` y los **55 slots de perk** de los tres jefes, y eso es un paquete propio. Cota superior medida quitándoles los ocho perks a rivales y jefes (recorte bruto, no calibración): buena 58,48 · hueco 9,31 · run **23,33** · `deathsPerRun` **1,68** · derrotas del acto 1 22,62 |
| AN-B | **CERRADA (ADR 0070, §35.2-§35.4).** Era ceguera estructural, y se corrige midiendo el perk **en campaña**: `--perk-values` juega ahora **ocho partidos consecutivos** arrastrando los contadores con el mismo `ApplyCounterDeltas` que usa la run. Ocho es el pico medio ponderado que el banco mide para los contadores de partido (7,4; mediana 7), y se mide en campaña y no cebando el contador porque cada uno crece a su ritmo: los de partido llegan a 7 y `silkyVeteranDribbles` se queda en 2,6. Medido con control (mismo instrumento, arrastre apagado): la campaña mueve **61,4 unidades de media en los quince perks del eje y 0,0 en los otros 36**, bit a bit. `clean_sheet_legacy` pasa de **−42 a +247** y la doctrina contextual pasa de comprarlo en el 0,2% de las runs al **10,9%** | **Cerrada.** `data/economy/perk-values.json` regenerado con 3.072 partidos por perk y desviación de fila 17 (antes 23) sobre una dispersión de 73 (antes 50). Cuesta: la run de la build buena baja de 19,42 a 16,25, pero de esos 3,17 puntos **4,67 son de que la tabla llevaba diez paquetes sin regenerarse** y la campaña **devuelve +1,50**. Lo que queda es **AS-A** |
| AN-C | **Las cuatro acciones muertas lo están a propósito** (§30.4 y §30.5). `MarkOpponent` 0,75%, `Block` 0,06%, `PressCarrier` 0,05% y `OfferSupport` **0,00%** de las decisiones; pierden por 473-845 puntos y su mejor score histórico está por debajo del score **medio** del ganador. `Block` y `PressCarrier` además están **descartadas** el 75% y el 82% de las veces por falta de objetivo legal, no por peso. Subirlas lo justo para que ganen rompe RT-056 en las cuatro, y en tres de ellas por el canal de la **entrada y la lesión** (ADR 0048): `MarkOpponent` ×3 deja las entradas en 0,55 y `PressCarrier` ×3 en 2,95, sobre una banda de 6-14 | **Cerrada como hipótesis** (ADR 0063 punto 2): "los perks desbloquean comportamiento" no se sostiene sobre estas cuatro acciones. Si se retoma, no es un paquete de perks sino de motor —una acción **nueva** que no compita con `Tackle` ni con `FindSpace`— y con revalidación completa de los cinco paquetes anteriores |
| AO-A | **CERRADA COMO PALANCA (ADR 0065, §32).** La discriminación por puerta **no se puede comprar**: los cuatro tipos de modificador de jefe borran build (`singleCopy` y `markStar` sólo tocan el escalón `muy buena`; el `pushBack` del jefe final está **inerte**; el `banChannel` cuesta **8,3 puntos** a la celda `buena` y **cero** a la `correcta`), así que lo único que queda es la dificultad, y la dificultad mueve a los dos perfiles a la vez. Medido en el campo: `the_hunt` ablandado al máximo que la tabla permite (calidad 46 → 42) deja la run buena en **19,83** y el suelo en **12,58**. La frontera: con la buena al 20% el suelo mínimo es **13,29%** (12,17% con la puerta 3 empinada) y con el suelo al 10% la buena máxima es **15,63%** (16,96%). Y **un cuarto evento filtro no la desplaza**: 15,62-16,82% según su razón. *(Enunciado original, ADR 0064)* la run es el producto de las tres puertas y cada una discrimina 1,15 cuando harían falta 1,26 | **Cerrada.** Y **revisar la tabla de la ADR 0033 no desbloquea nada**: la frontera sin restricción de tabla es la misma. Lo que falta no es permiso para mover celdas, es **razón de cuotas**: hay que elevar las tres a la potencia **1,622** (1,262·1,462·1,309 → 1,459·1,852·1,548), es decir que el hueco de la puerta 2 pase de 9,1 a **14,3 puntos**. Eso es fuerza de catálogo, **AL-A** |
| AO-B | **Una palanca separa perfiles sólo si cumple DOS condiciones** (ADR 0064): **(a)** la oposición no tiene ese número y **(b)** la build buena del jugador tampoco. Las seis palancas de las ADR 0055-0063 fallan por (a) —oro, valor del perk, rareza, fuerza del rival, peso de los atributos y ámbito del premio los cobra también el rival—. La séptima, el precio de perder un partido ordinario, **cumple (a) y falla por (b)**: la build buena pierde 4,03 ordinarios por run y la que no construye 4,03 | **Anotado, no abierto.** Es el criterio con el que descartar una palanca **antes** de medirla, y explica los siete paquetes con una frase. El castigo del perk mal puesto (ADR 0060) sigue siendo lo único medido que cumple las dos, y por eso sigue siendo el 60% del hueco  **La ADR 0065 le añade un tercer modo de fallo**: **(c)** el número tiene que ser de **uno** de los dos perfiles del jugador. La dificultad de la puerta la sufren los dos por igual, así que es un mando de dificultad —como la fuerza del rival (0058), el peso de los atributos (0061) y el ámbito del premio (0063)—. **La puerta amplifica el hueco que ya existe; no lo crea** |
| AO-C | **Encarecer la derrota ordinaria está medido y falsificado** (ADR 0064, §31.3-§31.6). Las tres builds pierden **4,03 / 4,16 / 4,03** partidos ordinarios por run —el equipo sin build pierde a mayor ritmo pero juega menos, y la truncadura compensa—, así que un castigo de D por derrota es un impuesto uniforme de 4·D. Perder ya cuesta **44 de oro y 4 recompensas por run**, el 34-39% de la economía, y las tres pagan la misma factura. Medido en cinco magnitudes (+3/+6/+12 de oro, −25/−50/−75% del oro en mano): el suelo se queda en 10,7 **sin orden** (8,67 con +6 y 11,17 con +12), la build buena no se mueve en el acto 2 y **baja** su tasa de victoria de la run, la build mala no tiene tendencia, y suben las derrotas del acto 1 (a 30,2-31,3 sobre un techo de 29,74) mientras el hueco cae a 9,71 con −75% | **Cerrada como palanca.** La causa está medida: el castigo se cobra en **la moneda que sólo tiene quien construye**. El perfil sin build acaba con ~55 de oro inútil (22 sin gastar + 33 en rerolls) y 1,6 perks; la build buena con 11 de oro sobre 86 y 12,1 perks. La palanca es **regresiva**, y lo es para cualquier denominación pagable (oro, clínica, matrícula, perder un perk, perder un objeto). El instrumento (`economy.defeatGoldPenalty`, `defeatGoldPenaltyPercent`, `runs-nomarket.csv`) se queda **en cero** para poder remedir |
| AO-D | **CERRADA (ADR 0066, §32.7): decisión del revisor, la métrica mide lo que su ADR describe.** `winRateAct{n}` y `matchesLostAct{n}` cuentan sólo partidos **ordinarios** y las cifras viejas se publican al lado (`winRateAct{n}_withBoss`, `matchesLostAct{n}_withBoss`). La separación es exacta: `RunPlayResult` gana `BossWinsByAct` junto al `BossSamplesByAct` que ya tenía. *(Enunciado original)* la métrica incluía el partido de jefe mientras la tabla de la ADR 0056 dice "partidos ordinarios (RF-002c)" | **Cerrada.** Build buena, acto 2: **60,33** ordinarios (ET 0,85) frente a 57,97 con jefe; acto 3: 43,30 frente a 44,43. **El objetivo 1 pasa a estar alcanzado en el acto 2** y sigue lejos en el 3. Precio anotado y no compensado: el hueco del acto 2 pasa de 10,03 (ET 0,50) a **9,91** (ET 0,87) sobre un suelo de 9,8 |
| AP-A | **Los cuatro tipos de modificador de jefe borran build** (ADR 0065, §32.3): `singleCopy` borra copias, `markStar` borra al portador principal, `banChannel` borra un canal y `pushBack` rompe la colocación —y romper la colocación hace que los perks bien puestos paguen su `elseEffects`, que es el canal de la ADR 0060—. Por construcción cobran más a quien más ha construido, así que **la puerta no se puede endurecer sin cobrarle al que construye bien**. Y dos de los tres que hay están **inertes** en los tres escalones que la tabla usa | **Abierta.** Un modificador que cambie las reglas del **campo** sin tocar el once del jugador —portería, reloj, turba— sería el primero con signo neutro, y es lo único que subiría la razón de cuotas desde el lado del jefe. También: `iron_curtain` (`pushBack` columna 6) no mueve a nadie con las alineaciones de hoy y el jugador lo ve anunciado en el informe de ojeo |
| AP-B | **`defeatShareAct1 ≤ 29,74%` y `runWinRate ∈ [20,30]` son incompatibles por aritmética** (ADR 0065, §32.6). `defeatShareAct1` es la **cuota** de runs perdidas que se pierden en el acto 1, `(1 − P₁)/(1 − producto)`, así que sube sola al subir la tasa de victoria aunque el acto 1 no se toque: 29,74 hoy, 30,84 con la run al 20% y `P₁` donde está | **Decisión del revisor.** Para cumplir las dos hace falta `P₁ ≥ 76,21` (hoy 75,33), o leer el guardarraíl como una **tasa** de derrota ordinaria del acto 1 y no como una cuota de las derrotas de run. No se ha compensado ablandando el jefe del acto 1 |
| AP-C | **La política no lee el informe de ojeo del jefe** (ADR 0065, §32.10). `RunPolicy` pasa `BossRuleModifiers` al sistema de jefes y **nunca lo consulta** al componer la alineación ni al repartir perks, así que la build automática no puede prepararse contra el modificador —que es justo lo que RF-012b y RF-014 le dan a un jugador humano— | **Abierta, y es un límite del instrumento, no del juego.** Todo lo medido sobre los modificadores de jefe es una **cota inferior de su valor de diseño**: se miden como impuesto puro sobre quien más build lleva. Antes de tocar un modificador *por lo que se mide hoy*, hay que enseñárselo a la política |

## Abierto tras medir el recorrido del catálogo (ADR 0067 y ADR 0068, `fase2-diseno.md` §33)

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AP-B | **CERRADA (ADR 0067).** El guardarraíl del acto 1 pasa de `defeatShareAct1 ≤ 29,74%` —una **cuota**, incompatible por aritmética con `runWinRate ∈ [20,30]`— a **`ordinaryDefeatRateAct1 ≤ 30,0`**, la tasa de derrota en partidos **ordinarios** del acto 1: numerador y denominador dentro del acto, así que no se mueve cuando cambian los actos 2 y 3. El techo se deriva de la ADR 0043 (el acto 1 es el **taller**) y de la ADR 0054 (el mejor equipo gana el **70-88%**), no de lo que hoy mide: **24,90** sobre 1.200 runs, 23,26 en la muestra de 60 de la puerta. La cuota se publica al lado como INFO, y con ella entran `ordinaryDefeatRateAct{2,3}` (39,67 y 56,70) y `bossWinRateAct{1,2,3}`, las tres puertas exactas | **Cerrada.** Es la primera métrica de acto con banda y entra en `FullRunGateTests` (`Act1IsTheWorkshop`). El encargo siguiente vigila el acto 1 con ella, no con la cuota |
| AQ-A | **CERRADA (ADR 0069): el eje de acumulación está gastado, y la recalibración de jefes que hacía falta no hacía falta.** Sube al techo de rareza el contador de **seis** de los siete efectos de los canales con recorrido —`battle_reader` (poco común, `intercept`, 50→**100 con `maxValue` 4**), `lane_reader` (30→100), `captains_voice` (30→100), `deathless_march` (30→**100**, un escalón bajo su techo), `pit_veteran` (30→50) y `silky_veteran` (50→100)— y **no toca ningún jefe**. Medido sobre el banco de 1.200 runs: **run 17,00 → 19,42** (ET 1,26) con el **suelo en 10,58** (ET 0,55), hueco del acto 2 **9,91 → 10,75**, `S` 0,8933 → **1,1750** (potencia **1,315**, más que el 1,276 del techo completo) y las **doce celdas de la ADR 0033 dentro de banda sin usar el margen de medida** — la `buena` del jefe final pasa de 40,2 (clavada en su suelo desde la ADR 0049) a **43,9** | **Cerrada.** Los objetivos 4 y 5 siguen siendo los dos extremos de la misma frontera y el punto entregado está **sobre** ella (el modelo predice 18,88 con suelo 10,58 y el banco mide 19,42): no queda intercambio, sólo más `S`, y el eje ya no lo da |
| AQ-B | **MATIZADA (ADR 0069): no eran el mismo punto del eje, eran los mismos DOS perks.** La ADR 0068 leyó el desbordamiento de las cuatro celdas como "el escalón `muy buena` y la build de la run son el mismo punto del eje". Medido perk a perk (§34.2): de los siete efectos que el techo mueve, **las builds del instrumento sólo llevan dos** —`clean_sheet_legacy` y `battle_reader`—, y los otros cuatro reproducen la tabla base **al decimal, celda a celda**. `clean_sheet_legacy` mueve seis celdas de doce y saca las cuatro, y es el perk que la doctrina contextual compra **2 veces en 1.200 runs**. La dosis existía: dejar `clean_sheet_legacy` donde estaba y acortar `battle_reader` a `maxValue` 4 | **Cerrada.** La celda `muy buena` no acota el eje: acota los dos perks que el modelo de build declara. Lo que queda abierto es **AR-A**, que es el instrumento |
| AQ-C | **Quitarle el catálogo a la oposición BAJA la separación** (§33.4): con los 71 slots de perk de `data/rivals/` y `data/bosses/` fuera (salvo los letales), `S` cae de 0,884 a **0,695** mientras la run de la build buena sube al 37,67% y el suelo al 29,50%. Es el mando de dificultad más potente medido en nueve paquetes | **Cerrada como palanca.** Falsifica la lectura ingenua de "un premio que la oposición no pueda cobrar": la condición (a) de la ADR 0064 hay que leerla en positivo —darle al jugador un número que la oposición **no puede tener**—, no quitarle a la oposición uno que tiene |
| AQ-D | **El castigo no separa a la build buena del suelo: la separa de la mediocre** (§33.4). Vaciar los `elseEffects` de los 17 perks que los tienen **sube** `S` de 0,884 a **1,001**, y a la vez hunde el hueco de partido ordinario entre buena y mediocre de 9,91 a **4,22** (la medición de la ADR 0063 reproducida). El suelo no tiene build que castigar | **Anotado.** Precisa a qué objetivo sirve la palanca de la ADR 0060: al 2, no al 4 ni al 5. No cambia ninguna decisión |
| AR-A | **CERRADA (ADR 0070, §35.5).** Las veinte builds se reescriben contra el catálogo que la run entrega (listón: 15% de presencia en las 240 runs de esa raza), sin tocar la tabla de la ADR 0033, ningún jefe ni la densidad por acto. Tres hallazgos del instrumento por el camino: **(1)** el orden dentro de un slot decide qué se mide —`BuildDensity` conserva el último perk de cada slot, así que de los 14 de una `correcta` se miden 5 y de los 17 de una `muy buena`, 11: cambiar los perks sin cambiar el orden dejó las tres celdas `correcta` **idénticas al decimal**—; **(2)** colocar mal un perk no lo mata (el primer escalón incoherente subió de 18,4 a **44,0**): lo que hace incoherente a una build es el **castigo** de `elseEffects`; **(3)** cuántos acumuladores lleva una build está medido —3,24 por run, 4,67 en las ganadas, 83% en el once—, no elegido | **Cerrada.** Las doce celdas quedan en banda **sin usar el margen de ±2,5** y sin recalibrar ningún jefe. Quedan tres celdas ajustadas: `grimhold_guns` muy buena 95,0 sobre 95, `the_hunt` incoherente 13,3 sobre 15 y `the_hunt` correcta 36,9 sobre 35 |
| AR-B | **CERRADA (ADR 0071).** `CounterCeilingFor` se queda acotando **una unidad** —que es lo único que puede acotar— y su comentario se corrige; se añade `CounterLineCeilingFor(rareza, ámbitoDeEquipo)`, que acota `k^maxValue` y distingue el ámbito: individual ×8/×32/×64/×128 y de equipo un cuarto (ADR 0060 midió el ámbito en 2-4×). El ×32 de poco común no es un criterio nuevo: es el catálogo que la ADR 0069 midió y entregó, convertido en regla | **Cerrada.** El único perk del catálogo que el techo nuevo caza es `deathless_march`, que baja de `maxValue` 5 a 4 (línea ×32 → ×16). Medido con el instrumento en campaña: doblar de ×8 a ×16 compra 39 unidades y doblar de ×16 a ×32 compra 10, menos de un tercio del error de fila — el canal satura donde el techo corta. Banco completo: **no cuesta nada** (19,42 → 19,50, ET 1,29) |

## Abierto tras enderezar el instrumento (ADR 0070 y ADR 0071, `fase2-diseno.md` §35)

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AS-A | **CERRADA (ADR 0072).** El listón deja de ser una constante y pasa a ser el **coste de oportunidad medido** del slot: cuantil `1 − S/N` de lo que el pool ofrece, con `S` los slots libres del once (medido: el once tiene **15** y llega al jefe del acto 2 con 11,8 puestos) y `N` las ofertas cobrables por delante (medido: 3,0 por capa, el 77% cobrables, descontado por las tasas de paso 71,8% y 43,9%), y el valor medido corregido por el ruido de la tabla (`rowDeviation` 17 sobre dispersión 73 → factor 1,0573). **Con coste de oportunidad cero el listón sale −1, no 0**, que es exactamente el valor de `steady_hands` y `safety_net`, los dos perks que la ADR 0070 nombró. Además, una pieza de la línea perseguida se juzga con el **crédito del maestro entre sus piezas** (acotado: la primera versión eximía la línea entera y metía `spearpoint` a −141 en el 38% de las runs). Medido sobre 1.200 runs: run **16,25 → 20,33** (ET 0,68), `contextualAdvantage` **−0,25 → +3,83** (ET 0,67), `masterDivergence` 9,78 → 23,24 | **Cerrada.** El objetivo 6 entra en banda por primera vez. Queda **AT-A** (el oro del mercado no está en el listón porque no se puede sumar hoy) y **AT-C** (la tabla de valor sigue sin ser buen predictor: el barrido de listones constantes no tiene forma) |
| AS-B | **CERRADA (ADR 0073), y la respuesta no era una sola.** (1) La comparación que abrió la pregunta mezclaba la **media de los cuatro escalones** del modelo con **una sola doctrina** del banco. (2) **Perks: se había ido el juego** — la mayor parte del exceso era que la contextual no tenía listón de slot, y con la ADR 0072 el banco baja de 5,3/11,8/12,4 a **4,3/9,5/11,3**. (3) **Objetos: se había ido el modelo, con fecha** — la ADR 0055 puso `rewardItemWeight` a 0 en el paquete AI y midió los objetos por run cayendo de 4,13 a 2,12, y `groups.json` siguió declarando 2/4/6 titulares equipados cuando el banco mide 0,8/1,8/3,0 (1,0/1,9/3,2 en las runs ganadas). `actDensity` se remide con anclas medidas: `incoherent` ← gastadora, `correct` y `good` ← contextual con la **misma** densidad (la ADR 0033 los separa por calidad, no por piezas), `excellent` ← runs ganadas | **Cerrada.** Las doce celdas en banda sin tocar jefe ni banda, y dos de las tres ajustadas se despegan. `counterCap` **no** se remide. Queda **AT-B**: con 64 × 8 dos celdas se salen, con las densidades viejas y con las nuevas |
| AS-C | **Las builds imponen una sola etiqueta de estilo a los siete titulares** (ADR 0070 §7). Humano Neutral, elfo Fine, enano Bulwark, orco Brute, no muerto Cold; una plantilla de verdad las tiene mezcladas según `styleTagWeights`. Es lo que hace que la run compre `cold_focus` en el **47,6%** de las runs con el perk muerto casi siempre —lo compra porque mide +3 y no tiene `elseEffects`— y el instrumento no pueda verlo | **Anotada.** Es la última simplificación grande del modelo de build. Tocarla obliga a repensar cómo se garantiza `tagsRequired` al asignar un perk a un slot |

## Abierto tras el listón del slot y la densidad remedida (ADR 0072 y ADR 0073, `fase2-diseno.md` §36)

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AT-A | **CERRADA (ADR 0086), los dos pasos.** El listón del mercado debía ser el del slot MÁS el del oro y no se podía sumar (`ItemScale.ValueOf` en otra normalización que la tabla de perks). Paso 1: `--item-values` mide cada objeto con el mismo espejo que `--perk-values` (2.016 partidos/objeto): el **nivel** queda bien determinado (+37 milésimas, ET 4) pero la dispersión entre objetos (27) no se separa del ruido de fila (21), así que la tabla no ordena objeto a objeto. Paso 2: `ItemWorthASlot` aplica a la compra de mercado el mismo listón de coste de oportunidad que un perk (`WorthASlot`/`SlotBar`, ADR 0072), pero con el **nivel agregado** en vez del valor por objeto, por decisión explícita del revisor. Medido (1.200 runs × 2 semillas, gate contra gate forzado abierto): `itemsOnRoster` baja (1,88→1,28 y 1,85→1,23), `perksOnStarters` y `mastersReached` suben en las dos semillas (`mastersReached` +10,17 y +7,08), `runWinRate` sube en las dos pero con magnitud ruidosa (+1,00 y +0,16). `FullRunGateTests` 15/15 | **Cerrada.** Solo toca el mercado; el objeto de recompensa sigue sin listón (fuera de alcance). El efecto sobre `runWinRate` mueve el punto estimado en la dirección que **AV-A** necesita sin resolverla — ver esa entrada |
| AT-B | **CERRADA (ADR 0074), y de las dos celdas que avisaba sólo una lo estaba.** El tamaño de muestra se **deriva**: midiendo la varianza a 4 y a 8 partidos por plantilla sale que la varianza entre plantillas ya está promediada (`A ≈ 0`) y que la de la celda es **1,15 veces la binomial**, así que escala como 1/N; para que el margen de ±2,5 valga **tres** desviaciones hace falta ET ≤ 0,83, o sea **N ≥ 4.172** partidos por celda. Se adopta **64 × 16 = 5.120** (ET medido de una celda 0,20-0,89 sobre ocho semillas), con las doce celdas en paralelo: la puerta pasa de 33 s a **1 m 38 s** con ocho veces la muestra, bit a bit igual que en serie. Medidas así, **once celdas estaban dentro y una fuera**: `the_hunt` buena **58,30** (ET 0,71), 1,70 bajo su suelo. `grimhold_guns` muy buena mide **94,34** (ET 0,20) y **no estaba fuera**: el 95,00 de la ADR 0073 se reproduce al decimal pero es una desviación alta de 2.560 partidos. Se recalibra `the_hunt` (quality 46 → 44, elección maximin sobre el margen más estrecho) y **las doce quedan dentro** | **Cerrada.** El margen de ±2,5 **no se toca** y la tabla tampoco. Coste medido: `contextualAdvantage` 3,83 → **2,50** (−1,33 emparejado, ET 0,24), porque ablandar una puerta comprime la ventaja de construir; run 20,33 → 20,75 y suelo 10,92 → 11,33, ninguna significativa; hueco del acto 2 intacto. Quedan **AU-A**, **AU-B** y **AU-C** |
| AT-C | **La tabla de valor sigue sin ser un buen predictor de lo que gana una run** (ADR 0070 §4, ahora con más evidencia). El barrido de listones constantes **no tiene forma**: 30 → 20,92, 45 → 18,09, 60 → 18,58 sobre 1.200 runs cada uno. Si el orden de la tabla fuera fiel, la respuesta al listón sería monótona hasta su óptimo | **Abierta.** El listón derivado (20,33) cae dentro de esa meseta y no se ha elegido el 30 porque sería elegir un número porque sale bien (RT-057). Lo que hace falta es entender por qué el orden de la tabla no se traduce: candidatos medidos a mano son la colocación (`elseEffects`, ADR 0060), la etiqueta de estilo (**AS-C**) y que la tabla mide un perk sobre una plantilla que no lleva los otros |

## Abierto tras la muestra que resuelve el margen y la frontera remedida (ADR 0074 y ADR 0075, `fase2-diseno.md` §37)

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AU-A | **CERRADA (ADR 0078).** La banda 40-60 de `randomBuildNearNone` pasa a un **techo de ≤ 45**, el mismo de `badBuildsLoseToNone`, y la métrica a `randomBuildLosesToNone`. No es relajar: el techo **baja** de 60 a 45 y lo que se quita es el **suelo de 40**, que afirmaba que construir sin criterio no puede salir claramente peor que no construir — exactamente lo contrario de la decisión del revisor de la ADR 0056. El 45 no se inventa: es el de las builds malas, y §8 de `fase1-diseno.md` **ya listaba `human_random` entre las «malas a propósito»**. La serie temporal lo respalda: 50,80 → 49,3 → 55,62 → **40,62**, bajando a medida que la ADR 0060 implementaba el castigo del perk mal puesto | **Cerrada.** Margen nuevo: 45 − 40,62 = **4,0 desviaciones** (antes 0,56), y las cinco semillas pasan. Cierra además el techo de **AL-D**: la palanca del castigo ya no está acotada por esta métrica (su otro techo, `grimhold_guns/incoherente` sobre 20, sigue) |
| AU-B | **CERRADA EN NEGATIVO (ADR 0076), y su premisa era un artefacto de muestra.** `R₁` **nunca se cayó**: el 1,008 de la ADR 0075 sale de un banco de 1.200 runs cuyo error típico sobre `ln R₁` es **0,091**; con **14.544 runs por lado** vale **1,149** (`ln R₁` = 0,139 ± 0,026), y las primeras 300 runs de cada bloque del banco grande **reproducen el pequeño bit a bit**. La escalera de puertas queda con la forma que la ADR 0033 diseña —`ln R` = **0,139 / 0,385 / 0,495**, creciente y suave— en vez de la primera puerta muerta y la tercera desbocada sobre la que la ADR 0075 razonó. Y el experimento que AU-B pedía sale **al revés**: `--slot-bar-off`, que es literalmente el juego de antes de la ADR 0072 con sus 5,3 perks en el primer jefe, baja la puerta 1 de **72,14 a 70,85** (4,4 ET emparejado) y la run de **19,57 a 16,83** | **Cerrada.** El listón del slot no gastó la primera puerta, la **construyó**. La corrección derivada —ponderar el coste de oportunidad por la **exposición a puertas**, que es una omisión real de la fórmula de la ADR 0072— queda implementada y **apagada** (`--slot-gates`): sube la densidad a 4,76 y la puerta no se mueve mientras la build buena del acto 2 baja 2,47 puntos. Lo que falta vuelve a ser **AL-A**, y es **+32%** de `S`, no el +9,3% que la ADR 0075 calculó |
| AU-C | **El corredor del acto 2 es de 45 puntos y la escalera mide 49,6: cabe, sin holgura.** La fila del acto 2 de la ADR 0033 exige `buena ≥ 60` con `incoherente ≤ 15`, o sea **45 puntos** de escalera, y el catálogo da 45,6 a quality 46 y 49,6 a 44. No hay ninguna calidad que deje las dos celdas cómodas: a 44 el margen mínimo es +0,70 en `incoherente` y a 45 es +0,47 en `buena` | **Anotada.** Es el mismo problema Y-10 del acto 1 («el catálogo produce saltos más grandes de lo que la tabla admite») en la fila de al lado, y con el signo contrario: aquí la escalera es **justa**, no ancha. Si el catálogo se estrecha, `the_hunt` se queda sin calibración posible |
| AU-D | **CERRADA (ADR 0077).** `Act2GatePassPermille` pasa de 439 a **493**; `Act1GatePassPermille` se remide y **no se mueve** (71,75%, los mismos 718). **El punto fijo converge en una vuelta**: el residuo del acto 2 cae de 57 a 3,5 milésimas y ahí se queda, por debajo del ruido de la medida (ET 17 milésimas). Y no es una observación: la constante de Lipschitz se mide barriendo la creencia **de extremo a extremo** —de (450, 250) a (950, 900), con el listón del acto 1 recorriendo de 25 a 48— y la medición se mueve **1,7 y 3,5 milésimas**: **`L ≤ 0,03`**, único y estable por Banach | **Cerrada.** Por qué es tan plano: el listón es un **cuantil de 51 perks**, así que corregir diez puntos mueve `N` de 49 a 50 ofertas y **el listón sale 38 en los dos casos**; y aunque lo moviera, la puerta apenas responde (ADR 0076). Derivarlas en tiempo de ejecución movería el listón cero unidades, así que siguen en la política, con `--act1-pass` / `--act2-pass` para remedirlas |
| AV-A | **La tasa de victoria de la run no está donde tres paquetes la dieron, y su banda no es decidible.** Medido con **7.272 runs por lado** (ADR 0076; el cierre es el pool de dos bancos de 7.272, 14.544 en total), el estado que entregó la ADR 0074 vale **19,68 ± 0,47** y el de hoy **19,52 ± 0,33**, no los 20,75 que aquella ADR publicó con «ET 0,57» — que era la dispersión de cuatro bloques, no el error binomial (1,16). La ADR 0072 declaró el objetivo 6 «en banda por primera vez» sobre esa misma medida. **Nota (ADR 0086, AT-A paso 2)**: el gate de slot del objeto de mercado sube el punto estimado en dos semillas de 1.200 runs (+1,00 y +0,16 emparejado), en la dirección que esta entrada necesita, pero con magnitud demasiado ruidosa para decidir nada — la medida de 14.544 runs/lado no se ha repetido con el gate puesto | **Abierta, y no es de calibración.** El punto está a **0,48 puntos** del borde de la banda 20-30: para que eso valiera tres errores típicos harían falta del orden de **60.000 runs por lado**, unas tres horas de banco. Las salidas son dos y las dos son del revisor: **aceptar la banda como orientativa** y juzgar el objetivo por el punto estimado, o **rebajar la exigencia de resolución** para esta métrica y decirlo por escrito. Antes de decidir, valdría la pena remedir el banco de cierre con el gate de AT-A puesto: si el efecto se sostiene a esa escala, la pregunta podría cerrarse sola |
| AV-B | **La tabla de valor es un promedio de la run y la run decide acto por acto.** Desde la ADR 0070 `--perk-values` mide en **campaña de ocho partidos** con el contador recorriendo 0..7, y ése es el número que la política usa para decidir en la **capa 0 del acto 1**, donde el contador vale cero. Los 15 perks con `accumulatesAcrossMatches` son el **29%** del catálogo pero **el 56%** de los que pasan el listón del acto 1 —y los dos primeros de la tabla, `deathless_march` (308) y `clean_sheet_legacy` (247), son los dos del eje—, y la ADR 0070 midió que la campaña les añade **61,4 unidades de media** frente a **0,0** a los otros 36 | **CERRADA (ADR 0085, 8 sep 2026).** Implementada como curva de valor por horizonte (`valuesByHorizon`, 16 horizontes, columna 8 = `values`) y lectura al horizonte en la política y en el listón del slot, con `--values-flat` de control. Medida con tres semillas y 1.800 runs por brazo: la run **no se mueve** (21,28 frente a 21,39, signo cambiante), porque el 89 % de los perks se compran con siete o más partidos por delante, donde las curvas ya son planas. Se deja encendida por ser la lectura correcta a coste cero. `MatchesPerLayerPermille` remedido: 550, no 590 |

## Abierto tras paralelizar el arnés de medición (9 sep 2026)

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AX-A | **`CompiledCondition` no es reentrante, y `BossGateTests` llevaba tiempo midiendo con una carrera.** `Sim/Perks/ConditionCompiler.cs` guarda el contexto de la evaluación en curso en un campo de instancia (`_context`) y comparte una única `NCalc.Expression`; las condiciones compiladas viven dentro del `Catalog`. Dos hilos evaluando el mismo perk sobre el mismo catálogo se pisan: cambian activaciones, goles y victorias. `BossGateTests` hacía `Parallel.For` con un solo `Catalog` desde la ADR 0074, así que sus doce celdas **no eran reproducibles bit a bit** (medido: dos pases con catálogo compartido dan 17,5391 y 17,5195 en `grimhold_guns` incoherente contra 17,4609 correcto). La auditoría previa solo buscó campos estáticos mutables y por eso no lo vio | **Acotada y contenida, raíz abierta.** Amplitud máxima de la carrera sobre una celda agregada: **0,078 puntos** (4 partidos de 5.120), treinta veces bajo el margen de ±2,5 y por debajo del error típico de celda (0,20-0,89): ninguna conclusión de las ADR 0074/0083 cambia. El arnés (`/Balance` y `Sim.Tests`) usa ahora **un `Catalog` por hilo** (`Balance/BalanceCatalogs.cs`, `Sim.Tests/ThreadCatalogs.cs`) y las doce celdas coinciden al decimal con `/Balance --boss-gate`. Lo que queda es la raíz: hacer `CompiledCondition.Evaluate` reentrante es un cambio de `/Sim` que toca un invariante declarado y pide ADR; hasta entonces cualquier `Parallel.For` nuevo sobre un catálogo compartido (en `/Game`, en un test) vuelve a picar en silencio |
| AX-B | **Dos celdas de la puerta de jefes no cuadran con lo documentado, y no es la carrera.** Con el catálogo por hilo (bit a bit reproducible): `grimhold_guns` muy buena mide **93,98** frente al 94,34 documentado en AT-B/ADR 0074 (0,36 puntos, cinco veces la amplitud de la carrera, y esa celda no se movía nada con el catálogo compartido); `the_hunt` buena mide **65,35** frente al **58,30** de AT-B (siete puntos, noventa veces la carrera). Ambas siguen dentro de banda | **Anotada.** El 58,30 nace del estudio de muestra de AT-B, anterior al motor de AW-A..AW-T (ADR 0081-0084), que separa más a las builds: es casi seguro un cambio real de motor, no de medida, y encaja con que la ADR 0083 tuviera que bajar el suelo de la celda incoherente por la misma razón. Merece una remedición documentada de la tabla de la ADR 0033 con el motor actual antes de que otra ADR cite números de AT-B |

## Abierto tras sacar la muerte del saque (paquete AY paso 1, 9 sep 2026)

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AY-A | **El indicador de riesgo letal (RF-012c) dejó de ser exacto al mover la muerte a la jugada de contacto.** `RunEngine.LethalRisks` → `Lethality.MarkedRisks` reparte el riesgo suponiendo dos cosas que ya no se cumplen: que **cada portador activa** (antes disparaba en `MATCH_START`, seguro; ahora necesita hacer una entrada) y que **marca al eslabón más débil** del once (ahora la víctima es el rival al que entra). El número que enseña el eslabón débil pasa a ser un **techo** y el 0 que enseñan los demás pasa a ser un **suelo falso**: RF-012d prohíbe callarse un peligro que sí existe con la misma fuerza con la que prohíbe anunciar uno que no | **Anotada, no tocada.** La lectura exacta que sí sobrevive es *por jugador y condicionada*: `Lethality.ChanceAgainst` es, tal cual, la probabilidad de morir **si te toca la entrada**, y sigue dependiendo de las tres palancas de la ADR 0048 (estado, aguante, colocación). Cambiar el significado del número que ve el jugador es decisión de diseño con ADR, no ajuste: queda para el paso 4/5 del plan de perks positivos. Mientras tanto el indicador **sobreestima** al marcado, que es el lado seguro |

## Anotaciones del revisor tras la primera run jugada (6 sep 2026, build de Windows exportada desde WSL)

Primera partida a mano sobre la build de `tools/export-windows.sh`. Cada anotación está contrastada con requisitos, ADR y código; se apunta como mejora, no como decisión tomada. Las que cambian una regla de juego se marcan como **decisión del revisor**.

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AW-A | **La parada no lee la posición del portero.** El disparo vuela hasta la línea de gol sin que nadie pueda tocarlo (`TryIntercept` lo salta) y `ResolveShotArrival` resuelve con velocidad, fuerza, fatiga, técnica del tirador, calidad y distancia tirador→portería; `goalkeeper.Position` no entra. Un portero en la fila equivocada para igual que uno bien colocado, y uno adelantado "para" un balón que ya lo ha rebasado. Coincide con `fase0-diseno.md` §"Vuelo hasta la línea de gol", así que es deuda de diseño de fase 0 que el campo animado hace visible | **CERRADA (7 sep 2026): pasos 0, 1, 2 y 3 cerrados, paso 4 descartado.** El motor ya no resuelve la parada al llegar a la línea: durante el vuelo, en cuanto el portero rival está a menos de `save.reachCells` (0,9 casillas, mismo valor que `pass.interceptRadiusCells`) del balón, se disputa el duelo en ese tick, una sola vez por disparo (`MatchEngine.TryGoalkeeperReach`/`ResolveSaveDuel`); si lo pierde o nunca llega, el gol se emite al llegar a la línea, como siempre. La fórmula del duelo (ADR 0041/0050) no cambia ni un término, solo de sitio. Diagnóstico previo (20 partidos con traza) confirmó que `EvaluateCover` sí devuelve al portero a la recta portería→balón a tiempo: 0 goles sin duelo. Lote de 500×2 semillas: `goalsPerMatch` +0,7%/+2,7%, `scorelineShare` sigue IN en las dos, dentro de los criterios de parada del plan, así que **no** hizo falta el ajuste a `reachCells=1.2`. Efecto casi neutro porque el punto de mira sigue siendo el centro exacto de la portería (ver plan §8, decisión abierta 1: anchura de la portería) — la regla queda instalada y correcta pero solo mordería con un portero desplazado con frecuencia. `possessionChanges` en semilla 1 pasa de 24,87 (dentro) a 25,10 (fuera), +0,23, dentro de la tolerancia ±1 del plan y del mismo desvío ya aceptado en AW-D/AW-E (posessionChanges); sigue pendiente la decisión de banda (plan §8, decisión abierta 2). **Paso 2 cerrado (7 sep 2026): la estirada.** Segundo radio `save.diveReachCells` (1,5 casillas, mayor que `reachCells`), que solo se intenta en el tick de llegada del balón a la línea y únicamente si el radio normal nunca disputó el duelo durante el vuelo; se resuelve con `save.divePenaltyPercent` (20) restado de `savePercent` antes del acotado 5-95, sin tocar el resto de la fórmula. En el lote de referencia (500×2 semillas) el resultado es bit a bit idéntico al del paso 1: la banda entre 0,9 y 1,5 casillas casi no se activa con los equipos y disparos de `data/balance/reference.json` (un portero asentado ya está casi siempre dentro de `reachCells`, y el desplazado suele caer fuera incluso de `diveReachCells`), así que no hizo falta el ajuste a `divePenaltyPercent=30`. Probado en aislado con un test dedicado (`ADivingGoalkeeperSavesShotsOutsideTheNormalReach`, `reachCells=0`) que confirma que la rama sí funciona cuando se fuerza la condición. Quedan los pasos 3 (bloqueo de defensas) y 4 (pasillo de pase por tiempo). **Efecto colateral encontrado y corregido al cerrar el hito**: la suite completa (una vez, sobre `main`) reveló `RefereeAndAbilitiesTests.TheInitialCriterionChangesTheMatch` en rojo; el paso 1 desplaza el consumo de tiradas de RNG en el partido (menos duelos de parada cuando el portero no llega, como ya avisaba el propio plan), y el test medía el efecto del sesgo del árbitro sobre solo **una** falta de margen en sesenta partidos (30 semillas × 2 sesgos) porque el escenario `TestMatches.Brutal` casi siempre termina antes por forfeit (equipo `fragile` de exactamente cinco jugadores). Arreglado subiendo `Seeds` de 30 a 120 (margen medido: 13 faltas contra 0), sin tocar el escenario ni el motor: es RT-056 aplicado a la letra ("un test que falla por mala suerte es un test mal escrito"), no una regresión de balance **Paso 4 intentado y descartado (7 sep 2026): el pasillo de pase dependiente del tiempo no funciona a la escala de nuestra rejilla.** Implementado tal como describe el plan §6 (radio efectivo `PassLaneRadius + closingCellsPerCellTraveled × t × longitud`, `t` = proyección del rival sobre el segmento pasador→receptor), primero aplicado a `EvaluatePass` y `EvaluateFindSpace` a la vez con `closingCellsPerCellTraveled=0.5`: `passChainAvgLength` cayó bajo el suelo de 2 en las dos semillas (1,93/1,96) sin que `possessionChanges` mejorara como predecía la hipótesis (menos intercepciones). Acotado después a que solo lo use `EvaluatePass` sobre un receptor real (revertido `EvaluateFindSpace` al radio fijo, porque ahí el destino es la propia casilla candidata y su proyección está casi siempre en `t≈1`, ensanchando el radio sin freno con cualquier carrera avanzada) — no bastó: seguía bajo el suelo. Con el único ajuste que el plan permite (`closingCellsPerCellTraveled` 0,5→0,3) la semilla 1 entra (2,02) pero la 2 sigue fuera (1,97), y `possessionChanges` empeora en las dos semillas frente a la referencia del paso 2 (25,10/25,97 → 25,35/26,33) en vez de mejorar. Descartado sin commitear: el radio, incluso acotado al pase largo real, descarta demasiados receptores legítimos en una rejilla de solo 5 filas por 16 columnas — la premisa (SimpleSoccer, espacio continuo) no traduce bien a una cuadrícula tan pequeña donde 2-4 casillas de radio efectivo es una fracción enorme del campo. Queda como referencia investigada en `docs/plan-intercepcion-disparo.md` §6; una futura vuelta necesitaría repensar la escala (por ejemplo acotar el radio efectivo a un máximo absoluto, no solo bajar el factor) antes de volver a intentarlo **Paso 3 cerrado (7 sep 2026): bloqueo de tiros por defensas de campo.** `MatchEngine.TryBlockShot` es `TryIntercept` aplicado al disparo: mismo radio (`pass.interceptRadiusCells`), misma `InterceptChance`, reducida por `shot.blockChancePercent` (50 de partida), un intento por jugador y disparo (`Ball.BlockAttempted`), aplica a cualquier tiro (a puerta o no) y va antes que el portero en `UpdateFlight`. Un bloqueo deja el balón suelto donde estaba, no en posesión de quien bloquea (es un rebote); los contadores de `LaunchShot` no se deshacen. Nueva métrica INFO `blockRate` (bloqueos / tiros totales, no solo a puerta, porque el bloqueo ocurre antes de saber si el disparo iba dentro). Lote de 500×2 semillas: `possessionChanges` sube +0,41/+0,20 sobre la referencia del paso 2, dentro del margen aceptado (no hizo falta el ajuste a `blockChancePercent=30`); `scorelineShare` sigue IN en las dos semillas; `blockRate` ≈2,65% de los tiros (~0,35 bloqueos/partido), una palanca discreta y coherente con la cuota real. **Hallazgo colateral, no es una regresión de este paso**: `StatisticalTests.PossessionChangesAreInRange` y `NoMandatoryMetricIsOutOfRange` ya estaban en rojo en `HEAD` (confirmado en worktree limpio sobre `4ad9910`, mismo valor 25,23 fuera de 12-25); la ADR de banda de RT-056 (decisión abierta 2 del plan §8) deja de ser cosmética y pasa a ser lo que desbloquea el gate de CI/la suite completa. Con esto se cierran los cuatro pasos planificados de AW-A (0, 1, 2, 3; el 4 quedó descartado) |
| AW-B | **CERRADA (7 sep 2026).** El portero ya está exento del desplazamiento táctico del bloque: `UpdateBlockShift` fija su `EffectiveHome` a `HomeCenter` siempre (`!player.IsOutfield`), sin el `offset` del bloque que sí sigue moviendo al resto. Ya no paga la correa de una zona desplazada que no le correspondía por estar fuera de ella | **Cerrada.** Emparentada con AW-A, cerrada también: el portero ya no se desplaza de más, y desde AW-A pasos 1-2 la parada ya lee su posición |
| AW-C | **CERRADA (7 sep 2026).** Un disparo fallado ya no vuela a la esquina del campo: `MatchEngine.OffTargetShotTarget` lo manda junto al poste (1,25 casillas de desviación respecto al centro de la portería, conservando el lado), no a la bandera de córner. `ResolveShotArrival` sigue resolviendo siempre saque de puerta —no mira la posición del balón para decidir córner, así que el resultado sigue siendo coherente con el destino nuevo— | **Cerrada.** Lote de balance de 500 partidos sin ninguna métrica con puerta obligatoria fuera de rango |
| AW-D | **CERRADA (7 sep 2026).** `EvaluatePass` penaliza ahora un pase con avance negativo, con `passBackwardPenaltyPerCell` (`data/ai/weights.json`), pero **solo** cuando el pasador tiene el mismo carril libre que `EvaluateDribble` consultaría para regatear (`OpponentsAheadCount`, extraído y compartido entre las dos). La primera versión sin acotar penalizaba cualquier pase atrás y descompensó la circulación normal en el lote de balance (`possessionChanges`, `passChainAvgLength`, `shotsPerMatch` y `tacklesPerMatch` fuera de rango); acotada a la condición real de la anotación, tres de las cuatro vuelven dentro. La segunda mitad original —valorar profundidad en el regate— **no hizo falta**: penalizar el pase ya bastó | **Cerrada, con un desvío aceptado por el revisor.** `possessionChanges` queda justo por encima de su tope (24,68 → ~25,5, dos semillas), consistente y sin que bajar la magnitud de la penalización (80 → 50) lo mueva: parece un coste estructural real de regatear más a menudo cuando de verdad hay hueco, no ruido ni un número mal calibrado. Si algún día se quiere cerrar del todo, el tope de `possessionChanges` (RT-056) necesitaría su propio ADR, no un ajuste silencioso (RT-057) |
| AW-E | **CERRADA en su primera mitad (7 sep 2026).** `FindSpace` solo premiaba alejarse del rival, sin mirar si la casilla candidata ya tenía compañeros: dos jugadores podían converger en el mismo hueco. Nuevo `findSpaceCrowdedPenalty` (`data/ai/weights.json`, magnitud 50), mismo radio que `supportCrowdedPenalty` (`TeammatesNear`, compartido con `OfferSupport`). Medido: con 90 (mismo valor que `supportCrowdedPenalty`) `passChainAvgLength` volvía a salirse de rango; bajado a 50, dos semillas con todas las métricas obligatorias dentro salvo `possessionChanges`, en el mismo margen pequeño ya aceptado desde AW-D | **Queda abierta la segunda mitad**: el desplazamiento de bloque del defensa (4 columnas en posesión) y su recuperación tras perder el balón, que depende de **D-31 / ADR 0031** (la correa fuera del presupuesto sigue sin implementarse). No se ha tocado en este cambio |
| AW-F | **CERRADA (7 sep 2026), ampliada por el revisor.** El texto de `startZones`/`startFlanks` en `data/l10n/*/templates.json` ya no usa palabras del terreno de juego ("el tercio rival" → "su tercio adelantado", "la banda izquierda" → "cualquier fila de su izquierda"). Además el nombre de la zona dentro de la descripción de un perk (ficha, PERKS y HABILIDAD RACIAL) es **hovereable**: subrayado en color de acento, con **tooltip nativo** con la explicación completa (nuevas claves `...Hint` al lado de la frase corta) y **resalta esa franja y sólo esa** sobre la cuadrícula de colocación, en cualquier ficha, titular o suplente. `Sim.Perks.DescriptionGenerator` sigue devolviendo texto plano (no se tocó `/Sim`); el marcado BBCode lo hace `Game/Ui/ZoneHintText.cs`, presentación pura. Ayuda pasiva de solo ratón, sin camino de mando (el botón "Zonas del campo" ya lo cubre) | **Cerrada.** Verificado con captura de un hover real (`Input.WarpMouse` + tooltip nativo esperado, `equipo-zona-frase.png`), no un manejador llamado a mano |
| AW-G | **CERRADA (7 sep 2026).** `TeamScreen` muestra ahora el riesgo de muerte por titular (RF-012c), recalculado sobre la alineación que se está mirando en ese instante —incluida la previsualización de un jugador cogido, no solo la guardada—, con la misma fachada que Ojeo (`RunEngine.LethalRisks`). Solo se muestra con una run en curso y un nodo de partido elegido; sin nodo no hay rival del que salga el riesgo | **Cerrada.** Verificado con captura (`equipo-run.png`, vía `--tour`); ninguna de las diez capturas de `--screenshots` lo enseña porque la plantilla de pruebas no tiene run detrás |
| AW-H | **CERRADA a medias (7 sep 2026).** Un mismo perk ya no puede salir dos veces dentro de un mismo surtido: tanto `MarketOfferGenerator` como `RewardSystem` retiran el perk elegido del pool (y de sus pesos) antes de sortear la siguiente opción, verificado con 200 y 150 semillas respectivamente. Ni recompensa ni mercado penalizan todavía un perk que ya tiene alguien en plantilla (sólo se retira cuando **ningún** portador elegible lo puede tomar), y no hay historial de ofertas entre nodos en `RunState`. `flank_specialist` es común con frecuencia 150, por eso se ve tanto | **Queda abierta la segunda mitad.** Factor de peso <1 en `PerkPool.OfferWeight` por cada copia ya en plantilla. Con lote de balance porque toca la tabla de valor por acto (**AV-B**) |
| AW-I | **CERRADA (ADR 0079).** El revisor pidió 9, no 8. La plantilla inicial del club baja de 10 (7+3) a 9 (7+2, se quita el MID suplente); la capacidad base se queda en 10, así que hay un hueco libre en el primer mercado sin vender ni descartar a nadie. Medido: la semilla de referencia de `ARunCanBePlayedFromStartToFinish` deja de ganar con un jugador menos generado (RT-022 desplaza la secuencia), se cambia a una semilla que sí gana con el mismo rango de nodos y partidos | **Cerrada.** No se ha medido con `--full-runs` si la tasa de victoria agregada se mueve con un suplente menos; queda como pregunta abierta si se quiere cuantificar, no bloquea la decisión del revisor |
| AW-J | **CERRADA a medias (ADR 0080).** El mercado ya garantiza exactamente un portero entre sus fichajes de pago (mínimo y máximo uno, `market.goalkeeperOffers`); el resto de fichajes, canteranos y el mercenario se sortean solo entre posiciones de campo. La recompensa de partido no cambia: sigue sorteando las cuatro posiciones uniforme, no era la vía que la anotación señalaba | **Queda abierta la segunda mitad.** En `TeamScreen`, permitir arrastrar a cualquier jugador a la casilla del portero cuando no hay portero disponible y mostrar que juega fuera de posición; hoy W-9 ya resuelve esto en `/Sim` (el titular de campo de menor id) pero la interfaz no lo enseña ni deja elegir a quién |
| AW-K | **CERRADA (7 sep 2026).** La ficha ya muestra el objeto equipado: `TeamState.EquippedItemOf` resuelve `RunPlayer.Item` en `StandardRunSystems.Items` y `PlayerCard` enseña su nombre y descripción generada (`ItemDescriptions.Describe`), con el delta de cada atributo que toca en verde (sube) o rojo (baja), reutilizando `Style.LinkCreated`/`Style.LinkBroken`. Sin run detrás (equipo de pruebas, rival de ojeo) sigue sin dato: la fila se queda en "sin objeto", que ya no es un placeholder de fase 2 sino un estado real | **Cerrada.** Verificado con captura (`equipo-objeto.png`) |
| AW-L | **CERRADA (7 sep 2026).** Un suplente ya se coge y se suelta igual que un titular: `ActivateRosterCard` (compartido entre el clic en la ficha y el botón de acción del mando con foco en la plantilla) lo coge si nadie más está cogido, con la misma pista visual (`Selected`) que un titular cogido desde su casilla; soltarlo sustituye o añade con la lógica de `/Sim` que ya existía (`PlacementView.WithPlayerAt`) | **Cerrada.** Verificado con dos capturas (`equipo-suplente.png`, `equipo-sustitucion.png`) |
| AW-M | **CERRADA (7 sep 2026).** Media de atributos en la ficha: simple, sin ponderar por posición (`Sim.Model.Attributes.Average`, en `/Sim` para que `/Balance` la pueda medir igual que cualquier atributo), en la cabecera de `PlayerCard` | **Cerrada.** 3 tests unitarios de `Attributes.Average` |
| AW-N | **CERRADA (7 sep 2026).** Botón "Ver equipo" en `MarketScreen`, `RewardScreen` y `ReportScreen`, que deja escrito su propio nombre de escena en `Nav.ReturnTo` antes de navegar; el botón de vuelta de Equipo lo consume una sola vez y lo limpia, sin tocar su lógica de siempre (Ojeo/Mapa) cuando no hay desvío | **Cerrada.** El estado de selección local de Mercado y Recompensa (categoría elegida, portador previsto) se pierde al pasar por Equipo porque vive en la escena y no en `RunState`; ninguna decisión ya cobrada se pierde, solo la vista previa. Anotado, no arreglado |
| AW-O | **CERRADA (7 sep 2026).** El botón que empieza el partido ya no vive en la fila de navegación de arriba junto a "Volver" y "Alinear" —pulsable sin haber leído nada—: se movió, pegado con un separador al bloque de riesgo y avisos que se acaba de leer, en `Style.Accent`. El texto pasa de "Empezar partido" a **"Confirmar y jugar"**. No hizo falta modal nuevo | **Cerrada.** Verificado con captura (`ojeo.png`) |
| AW-P | **CERRADA (7 sep 2026).** El botón "Volver" pisaba el texto "Zonas del campo (Y / Z)" en `TeamScreen` cuando había una run activa: `AddBackButton` colocaba el botón en `x=940` y `BotonZonas` ocupa `x=900-1068` en `Equipo.tscn` | **Cerrada.** Reubicado junto al título superior (`x=1140, y=8`), lejos de la fila de botones y del borde del panel de campo. Verificado con captura regenerada (`equipo-run.png`, `--tour`) |
| AW-Q | **La línea defensiva no tiene techo, y por eso el delantero rival acampa a su espalda.** El revisor lo señaló tras jugar la build con AW-A pasos 0-2. `MatchEngine.UpdateBlockShift` desplaza las casillas-hogar del bloque `+4,0` casillas (de 16) en posesión y solo `-1,0` fuera de ella (`data/ai/weights.json`, bloque `blockShift`), sin comprobar nunca dónde está el propio defensa más retrasado ni el balón. Investigado en `docs/referencia-motores-futbol.md` §6.1/§6.4: gfootball y HELIOS-base sí calculan una "línea defensiva" (segundo rival más adelantado, excluido el más adelantado que suele ser el portero, máximo con la columna del balón, nunca por detrás de medio campo — aritmética entera pura, sin geometría cara) y la usan de dos formas independientes. **El fuera de juego pitado (falta) no es lo que evita el problema**: es el **recorte posicional** del delantero contra esa misma línea (`forceNoOffside`/`ElizaController::GetSupportPosition_ForceField`, gfootball; recorte de las once posiciones de formación, `Strategy::updateFormation`, HELIOS-base) lo que le quita la casilla-objetivo a espaldas de la defensa, y el **techo del propio bloque** (`offsideTrapX`, mínimo entre balón, balón a 700 ms, portador rival y el propio segundo hombre más retrasado + 4 m) lo que evita que la línea suba de más. Los dos mecanismos funcionarían igual sin ningún árbitro pitando nada | **CERRADA (7 sep 2026).** Dos mecanismos independientes en `Sim/Engine/Utility.cs`/`MatchEngine.cs`, ninguno cambia una regla de juego. `DefensiveLineColumn(players, ball, team)` (columna del defensa de campo propio más retrasado, o el balón si está aún más atrás, en el marco del propio equipo) es el techo del bloque propio en `UpdateBlockShift` — solo para defensas de campo, con un margen (`blockShiftLineMarginCells`, 1,5 casillas) que solo frena, nunca empuja. `OffsideLineColumn(players, ball, attackingTeam)` es una función **distinta**, no la misma línea leída al revés: mide en el marco del ATACANTE (`max` con el balón, no `min`), y es la que recorta la casilla candidata de `FindSpace` contra la línea rival (`findSpaceLineMarginCells`, 1,0 casilla). La distinción entre las dos se verificó con datos antes de commitear: una primera versión que reutilizaba `DefensiveLineColumn` al revés para el recorte del atacante dejaba al delantero clavado a la altura del balón en la jugada normal (balón por detrás de la defensa rival) y rompía el balance (`shotsPerMatch` 14,15→7,52 OUT, `injuriesPerMatch` 0,49→1,08 OUT). Con las dos funciones separadas, lote de 500×2 semillas con todo dentro de rango; el techo del bloque es casi gratis (solo muerde si un defensa se descuelga de verdad) y el recorte de `FindSpace` es quien produce casi todo el efecto: `shotsPerMatch` baja (14,15/14,56 → 10,92/10,07) y `tacklesPerMatch` sube (6,24/6,47 → 9,15/10,53, alejándose del suelo, descargando la preocupación de AW-T). `ballThirdMaxShare` sube en vez de bajar (43,86/42,03 → 45,72/44,30, sigue IN): quitarle al atacante la casilla a espaldas de la defensa concentra el juego más cerca del balón, no lo reparte. El silbato de fuera de juego como falta señalizable **sí** sería una regla de juego nueva, ausente de `docs/requisitos.md` y `docs/simulacion.md`: sigue sin implementarse, no se toca sin decisión explícita del revisor |
| AW-R | **Durante cualquier balón muerto (saque de banda, córner, de puerta, de centro) el equipo entero queda congelado, no solo el que reanuda.** El revisor lo señaló como jugadores que se quedan "apiñados en una banda" tras una parada. Causa localizada en el código, no hace falta investigarla más: `MatchEngine.Step()` (rama `if (_restartTicksLeft > 0)`) llama a `TickStateTimer` y baja enfriamientos, pero **salta por completo `UpdatePlayer`** —donde viven `Decide()` y el movimiento— para todos los jugadores durante los `data/sim/tuning.json` → `restart.*Ticks` (15 ticks = 1 s para banda/puerta/córner/centro, 45 ticks = 3 s para penalti). El equipo queda literalmente clavado en la posición exacta que tenía en el tick en que el balón murió. Investigado en `docs/referencia-motores-futbol.md` §6.2: los tres motores de referencia recolocan a todo el mundo durante la pausa, ninguno lo congela. gfootball teletransporta a todos a una formación de saque en el instante de abrir la reanudación (`TeamAIController::PrepareSetPiece`, reutilizando la misma maquinaria de casillas-hogar con límites propios por tipo de saque) y solo entonces los deja quietos, sin simular siquiera el tiempo muerto en modo sin animaciones; SimpleSoccer, al contrario, los sigue moviendo hacia su región durante toda la pausa y no reanuda hasta que los dos equipos están colocados | **CERRADA (7 sep 2026).** Al final se optó por la receta de SimpleSoccer, no la de gfootball que se había apuntado como más barata: `MatchEngine.Step()` fusiona las dos ramas del `if (_restartTicksLeft > 0)` y `UpdatePlayer` (decisión y movimiento normales) corre siempre, incluso durante la cuenta atrás de la reanudación; solo se salta `UpdateBall`/`CheckOutOfBounds` mientras el balón sigue aparcado. El ejecutor del saque no necesita ningún teletransporte nuevo: `TakeRestart` ya fija su posición a mano al resolver, así que quien acabe más cerca queda igual de bien colocado. Único ajuste necesario: `UtilityContext.BallDead` (fijado en `UpdateContextCaches` desde `_restartTicksLeft > 0`) quita el bono de "balón suelto" de `ChaseBall` para que nadie converja sobre un balón que no se va a mover; el resto de acciones (Tackle, Block, PressCarrier, FindSpace/OfferSupport) ya se autodescartan sin poseedor real o sin posesión de equipo. Verificado con traza en 50 partidos: 472 ventanas de reanudación, con hasta 2,42 casillas de movimiento de un jugador de campo (antes de este cambio, siempre 0). Lote de 500×3 semillas: `possessionChanges` y `ballThirdMaxShare` bajan un poco (el equipo llega más recompuesto a la reanudación, así que hay algo menos de tiempo de balón parado y menos alternancias sueltas), y `injuriesPerMatch` sube (0,76/0,49 → 0,86/0,59/0,55): una marcación más ajustada nada más reanudar produce algún duelo físico más. Se investigó a fondo antes de aceptarlo: se descartó empíricamente que fuera el enfriamiento de bloqueo (que ahora también baja durante la reanudación, una inconsistencia menor que este cambio corrige de paso) — revertido ese descuento solo como diagnóstico, `injuriesPerMatch` seguía en 0,87, así que no es la causa. Con tres semillas, solo la primera roza el techo de 0,80 (0,86); las otras dos quedan cómodas (0,59/0,55). Mismo patrón que `tacklesPerMatch` tras AW-T: se acepta como coste estructural real de un motor más fluido, a vigilar si un lote futuro lo acerca más al techo |
| AW-S | **Los defensas persiguen el balón en exceso.** El revisor lo señaló de nuevo tras jugar con AW-A pasos 0-2 (ya se había anotado como parte de AW-E, segunda mitad). Investigado en `docs/referencia-motores-futbol.md` §6.3: hoy `chaseBallNotNearestPenalty` (`data/ai/weights.json`, 300) es una **penalización**, no una condición: sobre `Defender.ChaseBall = 380 × 125` (táctica fuera de posesión), 300 puntos equivalen a unas 6,7 casillas de `chaseBallDistancePenaltyPerCell` (45/casilla), así que al segundo y al tercer defensa más cercanos les sigue saliendo a cuenta perseguir frente a `MarkOpponent` (`320 × 150`) o `CoverSpace` (`420 × 140`). Los tres motores de referencia (gfootball, HELIOS-base, SimpleSoccer) no penalizan: **descalifican** con una precondición dura — un solo perseguidor designado por equipo con histéresis (gfootball exige que un aspirante sea un 20% mejor en tiempo al balón para relevar al titular, `Team::Process`; HELIOS-base solo persigue si eres el más rápido del equipo en llegar o llegas en ≤3 ciclos, `Bhv_BasicMove::execute`; SimpleSoccer exige ser literalmente el más cercano, `isClosestTeamMemberToBall`), más como mucho un segundo cazador con condiciones adicionales (gfootball) y marcaje exclusivo con el marcador sacado del grupo para que nadie doble | **CERRADA (7 sep 2026).** `EvaluateChaseBall` descarta la acción (no la penaliza) si el jugador no es `ctx.NearestToBall[team]` (el perseguidor designado, ya recalculado una vez por tick) y no es el receptor previsto de un pase en vuelo (excepción del paquete E, conservada tal cual). `chaseBallNotNearestPenalty` se elimina por completo (código, `Catalog`/`DataLoader`, `data/ai/weights.json`, esquema): ya no queda ninguna resta que aplicar. **Sin histéresis en esta pasada** (decisión explícita): añadirla exige estado nuevo de "perseguidor designado persistente" por equipo, un cambio de arquitectura mayor que se deja para una vuelta futura si el lote de balance lo pide; `ctx.NearestToBall[team]` ya se recalcula una vez por tick sin oscilar de forma perceptible. Lote de 500×2 semillas: todas las métricas obligatorias dentro de rango en las dos semillas; `tacklesPerMatch` apenas se movió (9,15/10,53 → 9,19/10,41), señal de que la compuerta dura formaliza la regla sin cambiar mucho el agregado con este roster de referencia — los defensas no designados rara vez ganaban la utilidad de todos modos frente a `MarkOpponent`/`CoverSpace`. Único dato fuera de rango, ya documentado como ruido conocido de plantilla fija por semilla (`docs/balance.md`, "betterTeamWinRate mide dos plantillas concretas"): `betterTeamWinRate_60_vs_40` en 90,36 en la semilla 2 (banda 70-88), dentro de la desviación de ±6 puntos ya medida entre semillas; no es el gate oficial de RT-056, que corre 1.000 partidos en semilla 1 |
| AW-T | **CERRADA (7 sep 2026).** Al ganar o perder el balón, un jugador no volvía a decidir de inmediato: esperaba su turno programado (`tuning.decisionIntervalTicks`, cada 2 ticks, desfasado por su propio id), así que ejecutaba durante ese tick el objetivo de cuando no tenía el balón —normalmente ya alcanzado, porque `FindSpace`/`OfferSupport` suele llevar al jugador justo a ese punto antes de que el balón llegue—, y se veía "parado en el sitio". Detectado por el revisor tras jugar la build con AW-A pasos 0-3 | **Cerrada.** `MatchEngine.SetOwner` llama a `Decide()` de inmediato, tanto para quien gana el balón como para quien lo pierde (simétrico), en vez de esperar al próximo turno. `Decide()` no toca el balón ni consume aleatoriedad (RT-024), así que es seguro llamarla fuera del recorrido normal de `UpdatePlayer`: no cambia el determinismo, solo el objetivo de movimiento de ese jugador en ese tick. Verificado en 3 semillas de 500 partidos: `tacklesPerMatch` baja de forma estructural y consistente (8,39/8,65/— → 6,24/6,47/6,73), siempre dentro de la banda (suelo 6,00) pero con el margen más ajustado de todas las métricas obligatorias; se explica porque un receptor congelado era antes un blanco fácil de entrada, y al reaccionar de inmediato esa entrada gratuita desaparece. `passChainAvgLength` sube (2,01/2,03 → 2,19/2,23/2,49) y `possessionChanges` mejora en dos de las tres semillas (una sigue en el mismo desvío ya aceptado desde AW-D). No hay palanca de magnitud que ajustar (es un arreglo estructural, no un número de `/data`): se acepta el desplazamiento de `tacklesPerMatch` con el mismo criterio que ya se aplicó al desvío de `possessionChanges` en AW-D — coste estructural real de un motor más fluido, no ruido ni un número mal calibrado. A vigilar si algún lote futuro lo acerca más al suelo |

## Abierto tras el paquete AY: ningún perk negativo, la muerte en la entrada (9 sep 2026)

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AY-A | **El indicador de riesgo del ojeo (RF-012c, ADR 0048) es ahora un techo, no el número exacto.** `Lethality.MarkedRisks` suponía que el portador letal rival activaba seguro (disparaba en el saque) y marcaba al eslabón más débil del once. Desde AY paso 1 la tirada solo cae sobre el rival implicado en una entrada, así que el portador tiene que llegar a entrar y la víctima es quien recibe la entrada: el vector sobrestima al marcado y enseña 0 a los demás, que sí corren riesgo. `ChanceAgainst` (la probabilidad de morir **si te toca la entrada**) sigue siendo exacta | **Abierta, decisión de diseño con ADR.** Opciones: (a) mostrar `ChanceAgainst` por titular como «riesgo si recibe una entrada del portador» (exacto, honesto, más números); (b) esperar `ChanceAgainst × P(entrada)` con una tasa de entradas medida por posición (un solo número, estimación); (c) dejar el techo con la etiqueta «hasta». Las cinco condiciones de la ADR 0048 se apoyan en que el jugador vea el riesgo antes: hay que elegir antes de la siguiente build |
| AY-B | **`runWinRate` cae con la muerte en la entrada.** Tras el paso 1 solo: 17,08 / 18,00 (semillas 1 y 7, 1.200 runs) frente a 20,83 / 19,83 antes de AY. Los rivales de los actos 2 y 3 llevan `skullsplitter`/`marrow_thirst` y ahora tiran en cada entrada del portador. **Paso 5, paquete completo:** `runWinRate` **18,42 / 17,83**, `deathsPerRun` 1,70 / 1,81 (banda 1,5-3), `mastersReached` 26,7 / 28,1, `ordinaryDefeatRateAct1` 23,4 / 23,3: la muerte en la entrada cuesta ~3 puntos y los pasos 2-4 recuperan ~1 | **Abierta, se toca después de AZ.** Palanca: `lethalChance` de los dos perks de rival (1.950 y 1.500) — hay margen, las muertes están 0,2-0,3 por encima del suelo— o su frecuencia en `data/rivals/`. No antes de AZ porque las tandas 1-3 mueven el motor (saque, tiro ante el portero, falta que reanuda, física del pase) y habría que remedir dos veces. AV-A (la banda 20-30 no era decidible a 19,5) sigue siendo del revisor, ahora con un punto claramente por debajo |

| AY-E | **Con las tablas emparejadas (ADR 0087) el listón del objeto de mercado (ADR 0086) apenas muerde.** `item-values.json` remedido emparejado: nivel +40 (antes +37, `rowDeviation` 29); la tabla de perks se comprime (p75 26, p90 98) y el `SlotBar` queda por debajo de +40 en casi todos los nodos, así que `ItemWorthASlot` casi nunca descarta (test `TheSlotBarNeverClosesTheItemMarket`: 35 de 34 compras). El efecto medido en la ADR 0086 (`mastersReached` +10) se midió con las tablas sesgadas | **Anotada.** El mecanismo sigue siendo correcto —las dos columnas están por fin en la misma unidad— pero su efecto hay que remedirlo en el paso 5; si sale nulo, la ADR 0086 pasa a ser instrumento sin consecuencia, como la 0085 |
| AY-C | **El instrumento de la ADR 0070 mezclaba el efecto del perk con el sesgo de su pareja de plantillas (±30 unidades por fila).** Descubierto en el paso 3: 18 perks «negativos» medían exactamente lo mismo que su control (mismas plantillas y semillas, sin el perk); `deathless_march` estaba infravalorado en 59. Corregido: valor emparejado (ADR 0087), `rowDeviation` 19 → 7 | **CERRADA (ADR 0087).** Consecuencia para AT-C: buena parte de «la tabla no predice» era ruido de pareja; la tabla emparejada tiene dispersión 37,6 sobre ruido 7 y ordena de verdad. AT-C se vuelve a mirar en el paso 5 con esta tabla |
| AY-D | **CERRADA.** Pasos 2-4 en `main` (`292799a`, `f34034f`, `71de64a`): rama `else` neutra, instrumento emparejado (ADR 0087), dos acumuladores con techo de tres pasos (`deathless_march` 303 → 178, `clean_sheet_legacy` 186 → 98), escalera desde 50 (ADR 0088) y dos bandas «muy buena» rebajadas (ADR 0089). Paso 5 medido: ver AY-B | **Cerrada.** 647 tests rápidos y 42 puertas en verde en Release. Quedan AY-A (indicador de riesgo) y AY-B (`runWinRate`) |

## Anotaciones del revisor tras la segunda run jugada (9 sep 2026)

| Id | Qué | Lectura aplicada / qué falta |
|---|---|---|
| AZ-A | **El saque de centro "va y vuelve".** «Hay un tick en el que el jugador va a sacar y vuelve al inicio y saca; antes funcionaba.» Sospecha: regresión de AW-R (todos los jugadores se actualizan durante el balón muerto) o AW-T (decisión inmediata al cambiar de posesión): el sacador se desplaza durante los ticks de reanudación y `ResolveRestart` lo devuelve al punto de saque | **CERRADA (AZ tanda 1).** El sacador se designa y se congela al abrir la reanudación (`BeginRestart` → `SelectTaker`), se salta en `Step` mientras dura la cuenta atrás, y la formación del saque de centro se recompone al pitar (`ResetPositions` en `BeginRestart`), no un segundo después. Test `TheRestartTakerStandsStillDuringTheDeadBall`. Sin datos |
| AZ-B | **Pases y tiros no miran la trayectoria, y el receptor recibe donde está.** «Solo se puede recibir un pase si el balón llega a donde está el receptor o si se ha lanzado en profundidad para recibirlo en carrera.» El balón es un punto sin colisión (RT-016); la intercepción es una tirada por radio y tick (`TryIntercept`, `TryBlockShot`). El paso 4 de `plan-intercepcion-disparo.md` (pasillo dependiente del tiempo) se descartó por descartar demasiados receptores en 5×16 | **Implementada y medida (tanda 3, pasos 0-5; ADR 0091), pendiente del revisor.** Intercepciones 4,8 → 8,5 %, pases sueltos 37 → 31 %, cadenas 2,01 → 2,21, tiros bloqueados 2,1 → 1,3 %, `passBeatenRate` 0,8-1,0 %. Fuera de banda tras la tanda: `shotsPerMatch` 7,25 / 6,64 (el pase en profundidad baja los tiros: regla de parada del plan), `ballThirdMaxShare` 52,0 / 49,9, `injuriesPerMatch` 0,97 / 0,82, `betterTeamWinRate` 88,3 / 75,4, `elf_none` 60,95. Ajuste del revisor aplicado (pase más profundo: 2-6 casillas, zona libre en el último tercio, el balón se para donde cae; ADR 0091 §último): tiros 7,72 / 7,12, goles 2,40 / 2,13 (recuperados). Lo que sigue fuera se investiga sin tocar bandas. Sin push |
| AZ-C | **Solo ante el portero, pasa atrás.** «Sigue pasando; es muy frustrante.» AW-D/AW-E ya tocaron disparo contra pase; el caso persiste con línea limpia a portería | **CERRADA (AZ tanda 1).** Precondición dura en `EvaluatePass`: con línea limpia a portería dentro del alcance de tiro (ningún rival de campo a menos de `pass.interceptRadiusCells` del segmento) **y ningún rival de campo por delante** (`OpponentsAheadCount == 0`, el sentido literal de «solo ante el portero»), un receptor por detrás no es destino legal y `Shoot` gana. El portero deja de contar como rival por delante y como presión. Con la precondición sobre todo el alcance de tiro `passChainAvgLength` caía a 1,93; con la mitad «ningún rival por delante», 2,06 / 2,19. Medido (2.000 partidos × 2 semillas): `shotsPerMatch` 10,04 / 7,47 → 10,08 / 8,32, `possessionChanges` 24,0 / 22,8 → 24,6 / 23,3, `injuriesPerMatch` 0,91 / 0,74 → 0,86 / 0,78, todo en banda. **Rojo aceptado por el revisor hasta la tanda 3:** `buildsWinDifferently_passChain` 1,10 frente al umbral 1,11 (ADR 0062); la física del pase (tanda 3) la moverá otra vez y el umbral se decide entonces con todo junto. Sin push mientras la puerta esté en rojo |
| AZ-D | **Falta pitada sin cambio de posesión.** «Veo "falta" en el log pero no cambia de posesión; un árbitro que pita falta debe parar el juego y darle el balón al rival.» Hoy `ResolveFoul` derriba al infractor, mueve el criterio, tira tarjeta y penalti, y el juego sigue: **RF-054** dice que solo penalti y roja detienen el partido | **CERRADA (ADR 0090, AZ tanda 2).** `RestartKind.FreeKick`: la falta señalada reanuda con el balón para el equipo que la sufre en el punto de la falta (RF-053, no RF-054), cuenta atrás de 8 ticks y barrera de 2 casillas (`restart.freeKickTicks`, `freeKickClearanceCells`); el saque se abre tras resolver la lesión de la entrada; si hay penalti, manda el penalti. Test `AWhistledFoulRestartsWithAFreeKickForTheFouledTeamAndAnUnseenOneDoesNot`. Medido: `possessionChanges` 23,9 / 23,2, `passChainAvgLength` 2,01 / 2,13, `shotsPerMatch` 9,46 / 7,95, `injuriesPerMatch` 0,91 / 0,74 (los dos bordes partían de 8,32 y 0,86); puerta estadística en verde |
| AZ-E | **Porcentajes del árbitro explícitos: pita/ignora, amarilla/roja, lesiona/no lesiona.** Existen ya en `tuning.json`: `tackle.foulBase` 320 (+`foulStrengthFactor`), `yellowCardBase` 250 / `redCardBase` 10 (+bonus de entrada dura, +desplazamiento por criterio), `injury.onFoulBase` 60 (ADR 0041), `referee.penaltyOnFoulInArea` 8.000. Lo que **no** existe es "pita/ignora": una entrada que sale falta se pita siempre; la falta no vista solo ocurre si un perk la anula (`cancelEvent`, RF-063). Los rasgos de árbitro que la modulan (tuerto, permisivo, RF-061) son de fase 3 | **CERRADA (ADR 0090).** `referee.whistlePercent` 80: la falta no señalada (`Foul` con detalle `unseen`) derriba igual, no saca tarjeta, no reanuda, no da penalti y mueve el criterio como acción sucia no vista (RF-063). Tabla de los porcentajes del árbitro con su valor efectivo medido en la ADR 0090: 5,17 / 3,67 faltas por partido, 0,27 / 0,19 amarillas, 0,08 / 0,04 rojas (INFO nuevas en `summary.csv`: `foulsPerMatch`, `yellowCardsPerMatch`, `redCardsPerMatch`). Lesiones por falta frente a por entrada limpia: fila de `MatchReport` que no existe, pendiente si hace falta |
| AZ-F | **Sustitución obligatoria al lesionarse o morir, elegida por el jugador durante el partido; no hay cambios voluntarios.** Hoy `EventType.Substitution` existe pero **nunca se emite**: el lesionado sale (`LeavePitch`) y el equipo juega con uno menos hasta el forfeit por debajo de 5 (RF-002b). El banquillo (RF-005: 2 suplentes) nunca entra | **Decisión del revisor que toca dos pilares**: «toda la decisión ocurre entre partidos» (§1 de requisitos) y RF-054 (puntos de pausa). Además, `MatchScreen` reproduce una **traza precalculada** (RT-013/RT-014): una decisión a mitad de partido exige un motor **reanudable con puntos de decisión** — el partido se resuelve hasta la lesión, devuelve el estado, la pantalla muestra la ventana, y el motor continúa con el sustituto elegido; determinista igual (RT-024) porque la decisión pasa a ser parte de la entrada y del registro. `/Balance` y las puertas resuelven el punto con una política por defecto (orden de banquillo por posición). Exige ADR de arquitectura (`arquitectura.md`, superficie pública de `/Sim`) y modifica RF-054 (una tercera pausa: lesión/muerte con banquillo disponible). Sin banquillo disponible, el equipo sigue con uno menos como hoy |
| AZ-G | **La clínica debe poder curar también las lesiones leves.** Hoy `MedicalSystem.Treat` rechaza a un leve (`MedicalSystem.cs:24-29`): la clínica solo trata graves (lectura estricta de RF-092/RF-094). La leve dura exactamente **un partido** al −15 % acumulable (RF-091, `MatchResolution.cs:66-75`); la grave no tiene duración: bloquea la alineación hasta clínica o taller (RF-092) | **CERRADA (ADR 0090).** `MedicalSystem.Treat` acepta la leve a `economy.clinicMinorCost` (4; la grave sigue a 10); la política automática la paga solo para un titular del siguiente partido, guardando el precio de una grave. Test `TreatingAMinorInjuryCuresItAtTheMinorCost` |
